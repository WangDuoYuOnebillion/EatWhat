# 开发路线图 (Roadmap)

> **一个阶段 = 一个会话。** 不要在一个会话里推进多个阶段。
> 当前进度看 [STATE.md](STATE.md);总纲与红线看 [CLAUDE.md](CLAUDE.md)。

每个阶段都写明**验收标准**。验收没过就不算这个阶段完成,不要往下走。

---

## 阶段总览

| 阶段 | 主题 | 状态 | 依赖 | 预计会话 |
|---|---|---|---|---|
| **P0** | 调研 → 需求 → 构建 → 验收 → 文档基线 | ✅ 已完成 | — | 4 个(已用) |
| **P1** | **纯手机化改造**(砍桌面适配) | ⬜ 待开始 ← **下一个** | P0 | 1 |
| **P2** | 手机真机走查与修复 | ⬜ 待开始 | P1 | 1 |
| **P3** | 手机原生手感(手势 / 反馈 / 滚动) | ⬜ 待开始 | P2 | 1 |
| **P4** | 校验脚本落盘(`check.js`) | ⬜ 待开始 | 无(可提前) | 1 |
| **P5** | 菜谱库扩充与校对(153 → 200) | ⬜ 待开始 | P4 | 1–2 |
| **P6** | 添加到主屏 / PWA(**含一个待决问题**;元信息已就位,只缺图标) | ⬜ 待开始 | P1 | 1 |
| **P7** | v1.1 全量回归与收尾 | ⬜ 待开始 | 全部 | 1 |

**排序理由:** P1 先做,因为它会改动全局 CSS —— 放在后面会让 P2/P3 的真机测试和手势工作全部白测一遍。P4 与 P1/P2 无耦合,如果某次会话时间紧可以插进来做。

---

## P0 · 调研 → 构建 → 文档基线 ✅

已完成,归档记录见 [WORKLOG.md](WORKLOG.md) #001–#010。

产出:153 道菜 / 848 步骤 / 122 项食材的单文件应用,5 个 Tab,做菜模式,摆一摆配餐,购物清单,以及 PRD / DECISIONS / WORKLOG 三件套。

---

## P1 · 纯手机化改造 ✅ 已完成(会话 #6)

### 为什么

原方案是"移动优先 + 桌面自适应",维护两套布局。方向已收窄为**只做手机端**([ADR-019](DECISIONS.md))。
两套布局的成本不只是 CSS 行数 —— 每个新功能都要验两遍,而桌面那一遍是没人用的。

### 具体任务

1. **删除桌面断点。** `index.html` 三处:
   - `@media(min-width:640px)`(菜卡两列、食材格拉宽)
   - `@media(min-width:1024px)`(Tab 移顶部、详情变居中弹层、`--tabh:0`、内容区 1120px)
   - `@media(min-width:1024px)` 第二处(`.tabwrap` sticky 修正)
2. **引入「手机画框」。** 桌面浏览器打开时不该是拉满的宽屏,而是居中一条手机宽度:
   - `#app { max-width: 480px; margin: 0 auto; }`
   - 桌面下画框外的 `body` 底色压暗一档 + 画框两侧 1px 界线,让它看起来是刻意的而不是没做适配
3. **修正 fixed 元素的画框对齐**(这是最容易出错的一步)。以下元素都是 `position:fixed; left:0; right:0`,加了画框后会横跨整个桌面视口,必须一起收进画框:
   - `.tabbar`(底部 Tab)
   - `.stickycta`(底部主 CTA)
   - `.sheet`(详情抽屉)
   - `.scrim`(遮罩 —— 这个反而应该保持全屏)
   做法:统一用 `left:50%; transform:translateX(-50%); width:min(480px,100%)`,`.scrim` 除外。
4. **清理桌面残留。** `.stickycta .inner{max-width:1120px}` 是桌面遗留,画框已经限宽,可以去掉。
5. **保留** `@media(max-width:400px)` / `(max-width:344px)`(小屏字号收缩)和 `@media(min-width:700px)`(做菜模式大字号——这条服务的是横屏/大屏手机,不是桌面,**确认后再决定留删**)。
6. `viewport-fit=cover` 与 `env(safe-area-inset-*)` 已就位,不要动。

### 交付物

- 改动后的 `index.html`
- `PRD.md` §8「响应式」章节改写为「单一移动布局 + 桌面画框」
- `DECISIONS.md` 新增 ADR-019(本次会话已写)
- `WORKLOG.md` 追加一条

### 验收标准

| # | 项 | 标准 |
|---|---|---|
| 1 | 横向溢出 | **320 / 360 / 375 / 390 / 414 / 480px** 六个宽度下,7 个视图均 `docScrollW == docClientW`,零页面级溢出元素(横向滚动容器内的溢出属预期) |
| 2 | 可点性 | 用 `document.elementFromPoint()` 逐控件命中测试,6 个非模态视图 + 抽屉 + 做菜模式,**可达率 100%** |
| 3 | 覆盖物审计 | 扫描所有 `fixed`/`absolute` 且覆盖视口 ≥25% 的元素,`opacity<0.05` 时 `pointer-events` 必须为 `none`,**命中 0 个违规** |
| 4 | 画框对齐 | 桌面 **1280 / 1440px** 下,`.tabbar` / `.stickycta` / `.sheet` 的左右边界与 `#app` 画框**误差 ≤1px** |
| 5 | 无桌面断点 | `grep -c "min-width:1024px\|min-width:640px" index.html` 结果为 **0** |
| 6 | 功能不回归 | PRD §11 的 21 项功能断言全部仍然通过 |

> **测试方法注意**(踩过两次的坑,见 WORKLOG #006 / #010):
> - headless Chrome 的视口下限被钳在 500px,`--window-size=375` 拿不到真的 375。必须用**固定宽度 iframe 画框**约束内嵌文档,配 `--allow-file-access-from-files`。
> - 测时序敏感的东西(抽屉滑入)前先注入 `*{transition:none !important}`,否则测到的是过渡起始态。
> - **不要用 `element.click()` 判断可点性**,它绕过命中测试。

---

## P2 · 手机真机走查与修复 ← **下一个会话做这个**

### 为什么

到目前为止所有测试都在 headless Chrome 里跑。这些东西**只有真机能验**,而且都是移动端专属能力:

### 具体任务

必须在**真机**上逐项确认(需要用户配合提供机型/截图):

| 项 | 验什么 | 风险 |
|---|---|---|
| `file://` 下 localStorage | 数据能否持久 | iOS Safari 策略最严,可能直接不可用 → 已有 [ADR-016](DECISIONS.md) 兜底提示,但没在真机确认过 |
| Wake Lock(做菜模式屏幕常亮) | 是否生效 | iOS Safari 支持较晚,可能需要降级方案(如播放静音视频保持唤醒) |
| WebAudio 计时器提示音 | 静音键下、后台时是否响 | iOS 需要用户手势解锁 AudioContext |
| `env(safe-area-inset-bottom)` | 底部 Tab 是否被小白条压住 | 全屏/加到主屏两种模式表现不同 |
| 输入法遮挡 | 搜索框聚焦时是否被键盘挡住 | iOS 上 `fixed` 元素在键盘弹出时行为异常 |
| 滚动惯性与橡皮筋 | 抽屉内滚动是否带动页面 | 需要 `overscroll-behavior: contain` |
| 长列表滚动帧率 | 153 道菜列表是否掉帧 | 若掉帧,转 P3 处理 |

### 交付物

- 真机测试记录表(写进 `WORKLOG.md`,含机型 / 系统版本 / 逐项结果)
- 针对发现问题的修复
- 无法在真机修好的问题 → 明确写进 STATE.md 的待决问题,不要藏起来

### 验收标准

上表 7 项全部有**明确结论**(通过 / 已修 / 已降级 / 已记录为已知限制),不允许留"没测"。

---

## P3 · 手机原生手感

### 为什么

现在的交互是"能用的网页",不是"像 App"。手机上这个差距很明显。

### 具体任务

按收益排序,时间不够就砍后面的:

1. **抽屉下滑关闭手势。** `touchstart/move/end` 跟手位移,松手过阈值关闭,不过阈值弹回。现在只能点 ✕。
2. **`overscroll-behavior: contain`**。抽屉滚到底时不要带动背后页面。
3. **触觉反馈。** 勾选食材、完成步骤、计时结束时 `navigator.vibrate()`(Android 有效,iOS Safari 不支持,做优雅降级)。
4. **Tab 切换的位移过渡。** 现在是硬切。
5. **长列表性能。** 若 P2 实测掉帧:菜卡列表分页(首屏 20 道 + 触底加载)或简易虚拟滚动。**没实测到掉帧就不做**,别提前优化。
6. **做菜模式左右滑动翻步骤。** 现在只能点按钮。

### 验收标准

| # | 项 | 标准 |
|---|---|---|
| 1 | 手势 | 抽屉下滑关闭跟手无卡顿;误触率可接受(横向滑动不触发关闭) |
| 2 | 滚动隔离 | 抽屉滚到边界不带动背景页 |
| 3 | 帧率 | 长列表滚动 ≥50fps(真机实测) |
| 4 | 降级 | 所有手势都有等效的点按入口,不存在只能用手势完成的操作 |
| 5 | 不回归 | P1 的可点性与溢出验收全部重跑通过 |

---

## P4 · 校验脚本落盘 ✅ 已完成(会话 #6)

### 为什么

WORKLOG #005 提到写过 `check.js` 做数据完整性 + 覆盖度审计,**但仓库里没有这个文件**。
它当时是一次性用完就丢的。这意味着 PRD §10 的两条硬约束(32 个常用食材各 ≥4 道主料、零"勾了没菜"的食材)现在**没有任何机制守着**,加减菜谱会悄悄退化。

### 具体任务

把它写成仓库里的 `check.js`(Node 运行,从 `index.html` 里正则抽出 `ING` / `RECIPES` 再校验):

1. 引用完整性:菜谱引用的食材 id / 厨具 id 是否都存在
2. id 唯一性:菜谱 id、食材 id 无重复
3. 营养自洽:`nu` 的蛋白/脂肪/碳水折算热量与标称 kcal 偏差 ≤20%
4. 覆盖度:每个 `hot:1` 食材至少是 4 道菜的主料
5. 零孤儿:每个食材至少被 1 道菜用到
6. 单位规范:`q` 的单位在白名单内(g / 个 / 片 / 勺 / 适量 …)
7. 步骤完整:每道菜 ≥3 步,每步有耗时,每道菜有 1 条关键经验

### 交付物

- `check.js`
- `README.md` 补一节"改数据后怎么自检"
- 跑一次,把当前 153 道菜的实际结果记进 WORKLOG

### 验收标准

`node check.js` 对当前数据**零 error**;能故意造 6 类错误各一个并被准确报出。

---

## P5 · 菜谱库扩充与校对(153 → 200)

**依赖 P4** —— 没有校验脚本就扩库,等于一边加菜一边破坏覆盖度约束。

### 具体任务

1. 先跑 `check.js` 看当前覆盖度短板在哪,**按短板补菜**而不是随便加
2. 补到 200 道,优先补:一人食、10 分钟快手菜、剩菜改造、空气炸锅
3. 高频菜(炒菜 Top 20)的步骤与热量二次校对 —— 尤其**烹调用油有没有算进热量**(这是同类产品最常见的低估来源)
4. 每批加完立刻跑 `check.js`

### 验收标准

- 200 道 ±5;`check.js` 零 error
- 32 个常用食材仍全部 ≥4 道主料
- 新增菜的 `q` 均以 **3 人份**为基准、`nu` 为**每份**(红线第 8 条)
- 随机抽 10 道人工复核步骤可执行性

---

## P6 · 添加到主屏 / PWA

### ⚠️ 这里有一个必须先问用户的待决问题

PWA(manifest + service worker)**要求 https 或 localhost,`file://` 下 service worker 不可用**。
而本项目的核心卖点之一就是"双击 index.html 就能用"([ADR-001](DECISIONS.md))。

三条路,**开始这个阶段前必须让用户选一条**:

| 方案 | 得到什么 | 代价 |
|---|---|---|
| **A. 不做 PWA,只做 iOS「添加到主屏」** | iOS Safari 分享 → 添加到主屏,有图标、全屏运行 | 要先把文件放到一个能访问的地址;Android 体验弱一些 |
| **B. 部署到静态托管**(GitHub Pages / Vercel) | 完整 PWA:安装、离线缓存、启动图、独立窗口 | 破坏"纯本地单文件"的定位,且需要联网首次加载 |
| **C. 保持现状,只优化「加到主屏」的元信息** | 加 `apple-mobile-web-app-*` 系列 meta、图标、主题色 | 无 service worker,但本来就不需要 —— 应用已经零网络请求 |

**倾向 C**:这个应用没有任何网络请求,service worker 的离线缓存对它是多余的;真正缺的只是图标和全屏元信息。但这是产品定位问题,得用户拍板。

### 已经做掉的部分(2026-08-22 核对实现后确认)

`index.html` 的 `<head>` 里**已经有**全套元信息,不要重复做:

```html
<meta name="theme-color" content="#F5F0E8">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="apple-mobile-web-app-title" content="今天吃什么">
<meta name="format-detection" content="telephone=no,date=no,address=no,email=no">
```

也就是说**方案 C 只剩两件事**:图标 + 真机确认。

### 剩下的具体任务

1. **`apple-touch-icon`** —— 现在完全没有,加到主屏后会是一张网页截图缩略图,很难看。用内联 `data:` URI(红线:不引外部文件),180×180。
2. **全屏运行时的顶部安全区实测** —— `.topbar` 已写 `env(safe-area-inset-top)`,但**加到主屏的全屏模式**和普通 Safari 标签页的安全区行为不同,必须真机确认。
3. 若用户选了方案 A/B,再追加 `manifest.webmanifest` 与部署;选 C 则到此为止。

### 验收标准

真机加到主屏后:有正确图标与名称、全屏无浏览器地址栏、顶部不被刘海/灵动岛压住、底部不被小白条压住、冷启动数据仍在。

---

## P7 · v1.1 全量回归与收尾

1. 重跑 PRD §11 全部验收清单(功能 21 + 可点性 + 布局 + 视觉)
2. 重跑 `check.js`
3. 真机复测 P2 的 7 项
4. 文档全面对齐:PRD 回填实际数字、README 更新用户可见变化、DECISIONS 补齐、STATE.md 标记 v1.1 完成
5. 输出一份「已知限制」清单给用户

### 验收标准

所有验收项有实测结果;文档与实现零不一致;已知限制明确列出而非隐藏。

---

## 随时可做(不占阶段)

- 修 bug(用户报的问题优先于任何阶段任务)
- 加菜谱(遵守红线第 8 条 + 跑 `check.js`)
- 文档订正

## 明确不做(范围外)

账号 / 云同步 · 拍照识别食材 · 真实菜品照片 · 菜谱社区 · 买菜电商跳转 · 视频教程 · AI 动态生成菜谱 · **桌面端专属布局**

理由见 [PRD.md](PRD.md) §12 与 [DECISIONS.md](DECISIONS.md)。
