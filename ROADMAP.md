# 开发路线图 (Roadmap)

> **一个阶段 = 一个会话。** 不要在一个会话里推进多个阶段。
> 当前进度看 [STATE.md](STATE.md);总纲与红线看 [CLAUDE.md](CLAUDE.md)。

每个阶段都写明**验收标准**。验收没过就不算完成,不要往下走。

---

## 怎么用这份文档(分会话协作)

**会话开头**
1. 读 [CLAUDE.md](CLAUDE.md)(红线)+ [STATE.md](STATE.md)(现在在哪)
2. 从下面的「执行顺序」表里取**第一个未完成的阶段**
3. 读那个阶段的章节,向用户复述:「本次会话做 Pn:xxx,验收标准是 N 条,做完就停」
4. 阶段里若标着 ⚠️ **开工前必答**,先把问题问掉,不要边做边猜

**会话结尾(缺一不可)**
1. 跑该阶段的验收标准,把**实测结果**写下来(不是"应该没问题")
2. 追加 [WORKLOG.md](WORKLOG.md) 一条
3. **重写 [STATE.md](STATE.md)**
4. 有规格变化 → 回填 [PRD.md](PRD.md);有方向性选择 → 新增 ADR
5. `git commit`(pre-commit 会自动跑 `check.js`)
6. 告诉用户「本阶段完成,下个会话做 Px」

**关于编号:阶段号只增不改,永不重排。**
`WORKLOG.md` / `DECISIONS.md` / `STATE.md` 里到处引用 P1、P4,重排编号会让这些引用集体失效。
所以新阶段一律往后加号(P8、P9…),**真正的先后顺序看下面那张表**,不看编号大小。

---

## 执行顺序 ← **这才是「下一步做什么」的权威**

| 顺序 | 阶段 | 主题 | 状态 | 预计会话 |
|:--:|:--:|---|---|:--:|
| 1 | **P0** | 调研 → 需求 → 构建 → 验收 → 文档基线 | ✅ 会话 #1–#4 | 4(已用) |
| 2 | **P5.1** | 改名 + iOS 适配 + 修中文搜索 | ✅ 会话 #6 | 1(已用) |
| 3 | **P1** | 纯手机化改造(砍桌面适配) | ✅ 会话 #6 | 1(已用) |
| 4 | **P4** | 校验脚本落盘(`check.js`) | ✅ 会话 #6 | 1(已用) |
| 5 | **P8** | **部署到免费托管(Gitee Pages)** | 🟡 我这部分完成,待用户部署实测 | 1 |
| 6 | **P2** | 手机真机走查(部署后重新裁剪过) | ⬜ | 1 |
| 7 | **P6** | 加到主屏 / PWA | ⬜ | 1 |
| 8 | **P3** | 手机原生手感(手势 / 反馈 / 滚动) | ⬜ | 1 |
| 9 | **P5** | 菜谱库扩充与校对(153 → 200) | ⬜ | 1–2 |
| 10 | **P9** | 多端:微信小程序(**大工程,可选**) | ⬜ | 3–5 |
| 11 | **P7** | v1.1 全量回归与收尾 | ⬜ | 1 |

> 会话 #6 一口气做了三个阶段(P5.1 / P1 / P4)—— 那次是在治理文档建立之前开工的,属于流程债,不是常态。
> 从 P8 起严格一会话一阶段。

### 为什么把部署提到了第 5 位(顺序改过一次)

原顺序是 P1 → P2 真机 → P3 手感 → P6 主屏。现在把 **P8 部署**插到 P2 前面,三个理由:

1. **它是当前最卡的真实需求。** 用户要把应用发给别人用。而只发 `index.html` 这条路在 iPhone 上基本走不通 —— 微信不预览 `.html`,AirDrop 只能苹果之间用,普通人走不完"存文件 → 挑浏览器打开"这三步。
2. **不先部署,P2 会白测一半。** `file://` 下 iOS Safari 很可能禁 localStorage;托管到 https 之后这个问题**直接消失**。先测 `file://` 再部署,等于测一个马上要作废的环境。
3. **P6「加到主屏」从 `file://` 基本走不通**,托管之后才谈得上。

---

## 已完成阶段(归档)

### P0 · 调研 → 构建 → 文档基线 ✅ 会话 #1–#4

产出:153 道菜 / 848 步骤 / 122 项食材的单文件应用,5 个 Tab,做菜模式,摆一摆配餐,购物清单,以及 PRD / DECISIONS / WORKLOG 三件套。记录见 [WORKLOG](WORKLOG.md) #001–#010。

### P5.1 · 改名 + iOS 适配 + 修中文搜索 ✅ 会话 #6

改名「小付,今天吃什么?」;iOS 适配 13 项([ADR-021](DECISIONS.md));修掉中文输入法搜不了字的真 bug([ADR-020](DECISIONS.md))。记录见 [WORKLOG](WORKLOG.md) #012。

### P1 · 纯手机化改造 ✅ 会话 #6

删三处桌面断点、引入 480px 手机画框、四个 `fixed` 元素收进画框。六条验收标准全过。见 [ADR-019](DECISIONS.md) 与 [WORKLOG](WORKLOG.md) #012。

### P4 · 校验脚本落盘 ✅ 会话 #6

`check.js`(7 类校验 + `--selftest` 注入 8 类错误自验)+ pre-commit 钩子。见 [WORKLOG](WORKLOG.md) #013 / #014。

---

## P8 · 部署到免费托管(Gitee Pages)← **下一个会话做这个**

### 为什么

`index.html` 是完全自足的(零外链、零 fetch、252KB),但**分发方式是个死结**:

| 发送方式 | 结果 |
|---|---|
| 微信 / QQ 发文件 | 内置浏览器不预览 `.html`,要手动"存到文件 App → 挑浏览器打开",普通人走不完 |
| AirDrop | 能用,但只限苹果设备之间 |
| 邮件 / 网盘 | 同样要先下载再手动挑浏览器 |

托管成一个网址之后,这些一次性全部消失,而且顺带解决 localStorage 和「加到主屏」。

### ⚠️ 开工前必答(三个问题)

**1. 仓库要不要公开?**
Gitee Pages 免费版要求仓库公开 —— 意味着 `CLAUDE.md` / `STATE.md` / `WORKLOG.md` 这些内部文档也会公开。三个选择:

| 选项 | 做法 | 代价 |
|---|---|---|
| **A. 全部公开** | 直接推现有仓库 | 内部文档、踩坑记录全公开(内容本身不敏感) |
| **B. 单独一个部署仓库** | 只放 `index.html` 的公开仓库,开发仓库保持私有 | 每次更新要同步两个仓库 |
| **C. 同仓库单独分支** | `gh-pages` 风格,该分支只有 `index.html` | 分支管理稍复杂,但一个仓库搞定 |

倾向 **C** —— 一个仓库,内部文档不外露,更新只需推一个分支。

**2. 域名够用吗?** Gitee Pages 给的是 `用户名.gitee.io/仓库名` 这种地址。要自定义域名需要额外备案,先用默认的行不行?

**3. 万一 Gitee 不行,备选走哪个?** Gitee Pages 免费版需要**实名认证**,而且历史上服务政策调整过。备选:

| 平台 | 国内访问 | 免费 | 备注 |
|---|---|---|---|
| Gitee Pages | 好 | 是 | 需实名;免费版每次更新要手动点「更新」按钮 |
| 腾讯云 COS 静态网站 | 最好 | 否(近乎零成本) | 252KB 一个文件,月费几分钱 |
| Netlify / Vercel | 不稳 | 是 | 推送自动部署,体验最好 |
| GitHub Pages | 不稳 | 是 | 国内朋友可能打不开 |

### 用户必须自己做的(我做不了)

注册账号、实名认证、创建仓库、开启 Pages —— 这些都要登录和身份验证,我不能代劳。我会把每一步写成清单。

### 我能做的

1. **`apple-touch-icon`** —— 180×180 PNG,离线生成(headless Chrome 渲染 SVG → PNG),内联为 `data:` URI 或作为单独文件。这直接影响「加到主屏」是图标还是网页缩略图
2. **按选定的仓库方案整理结构**(B/C 需要额外的分支或目录布置)
3. **写 `DEPLOY.md`** —— 逐步部署清单 + 以后怎么更新
4. **核对托管环境下的行为** —— 确认没有任何依赖 `file://` 的假设

### 验收标准

| # | 项 | 标准 |
|---|---|---|
| 1 | 能访问 | 手机 Safari 打开链接,应用正常加载 |
| 2 | **微信内能开** | 微信里直接点链接就能用 —— 这才是"发给别人"的真实场景 |
| 3 | **数据能存住** | 勾几样食材 → 完全关掉浏览器 → 重开,数据还在(https 下应该没问题,但必须实测) |
| 4 | 无存储警告 | 页面顶部不出现「这个环境不让保存数据」红条 |
| 5 | 加到主屏有图标 | 不是网页缩略图 |
| 6 | 更新流程可复现 | 按 `DEPLOY.md` 走一遍改动 → 上线,能成功 |
| 7 | 不回归 | `check.js` 零 error |

### 会话 #7 实测结果(2026-08-24)

我能在本地验的都过了,真机相关的等用户部署后测。详见 [WORKLOG](WORKLOG.md) #016、[ADR-022](DECISIONS.md)、[DEPLOY.md](DEPLOY.md)。

| # | 项 | 我这边 | 待用户实测 |
|---|---|:--:|---|
| 1 | 能访问 | 本地 http 200 ✅ | 手机 Safari 打开线上地址 |
| 2 | 微信内能开 | — | 微信里点链接就能用 |
| 3 | 数据能存住 | 空→选→刷新恰好留存 ✅ | 真机上完全关掉浏览器再重开 |
| 4 | 无存储警告 | http 下无红条 ✅ | (https 下同理,顺带确认) |
| 5 | 加主屏有图标 | `apple-touch-icon.png` 就位、`<link>` 解析 ✅ | 真机「添加到主屏」看是不是棕碗图标 |
| 6 | 更新流程可复现 | 流程写死在 DEPLOY.md | 按它走一遍改动→上线 |
| 7 | 不回归 | `check.js` 0 error ✅ | — |

**产出:** `apple-touch-icon.png`、`DEPLOY.md`、本地 `gh-pages` 分支(方案 C)、`<head>` 图标链接。
**用户要做:** 注册/实名/建公开仓库/推送/开 Pages —— 清单见 [DEPLOY.md](DEPLOY.md)。做完后回来跑 1/2/3/5/6 真机验收,全过才关闭 P8。

---

## P2 · 手机真机走查(部署后重新裁剪)

> **这个阶段的内容因为 P8 提前而变了。** 托管到 https 之后,`file://` 相关的风险项直接作废,
> 剩下的是真正的移动端能力问题。开工前重读这一节,别照抄旧版。

### 已知设备

iPhone 13(系统版本待确认 —— 开工时先让用户在「设置 → 通用 → 关于本机」看一眼)。

### 逐项清单

| # | 验什么 | 怎么看 | 预期 |
|---|---|---|---|
| 1 | 数据持久化 | 勾食材 → 杀进程重开 | https 下应正常(P8 已验过一次,这里复核) |
| 2 | 计时提示音 | 做菜模式点「开始计时」等到 0 | 应响。已在点击手势里解锁 AudioContext([ADR-021](DECISIONS.md)) |
| 3 | 静音键 / 后台 | 手机静音时、切到后台时计时结束 | iOS 静音键下大概率不响 —— 若如此,考虑加视觉提示兜底 |
| 4 | Wake Lock | 做菜模式静置 | **预期会锁屏**(iOS 不支持 Wake Lock),只要不报错就算过 |
| 5 | 刘海 / 灵动岛 / 小白条 | 顶栏、底部 Tab | 已写 `env(safe-area-inset-*)`,需真机确认 |
| 6 | 软键盘 | 点搜索框 | 键盘弹起后能看到结果;吸底 CTA 应自动隐藏 |
| 7 | 长按不弹菜单 | 长按食材格 | 不应弹「拷贝/查询」和放大镜 |
| 8 | 中文输入法 | 用拼音搜「西红柿」 | 能正常打出汉字(这是会话 #6 修的 bug,必须真机复核) |
| 9 | 滚动帧率 | 153 道菜列表快速滑动 | 不掉帧;掉帧则转 P3 |
| 10 | 抽屉滚动隔离 | 菜谱详情滚到底继续拉 | 不应带动背景页 |

### 验收标准

10 项全部有**明确结论**(通过 / 已修 / 已降级 / 已记录为已知限制),不允许留"没测"。测试记录含机型与系统版本,写进 WORKLOG。

---

## P6 · 加到主屏 / PWA

> P8 完成后,原来那个「PWA 要 https,而我们是 file://」的待决问题**自动消失**了。
> 这个阶段因此简化很多。

### 元信息已就位,不要重复做

`<head>` 里已有 `theme-color` / `apple-mobile-web-app-capable` / `apple-mobile-web-app-status-bar-style` / `apple-mobile-web-app-title` / `format-detection`。

### 剩下的任务

1. `apple-touch-icon`(若 P8 已做完则跳过)
2. `manifest.webmanifest` —— 托管之后可以做了,给 Android 和桌面 Chrome 用
3. **要不要 service worker?** 应用零网络请求,离线缓存的唯一价值是"首次加载后断网还能开"。托管场景下这个价值不大,但成本也低。开工时问用户
4. 全屏模式下的顶部安全区实测 —— 加到主屏的全屏模式与普通标签页行为不同

### 验收标准

真机加到主屏后:正确图标与名称、全屏无地址栏、顶部不被刘海压住、底部不被小白条压住、冷启动数据仍在。

---

## P3 · 手机原生手感

### 具体任务(按收益排序,时间不够砍后面的)

1. **抽屉下滑关闭手势** —— `touchstart/move/end` 跟手位移,松手过阈值关闭。现在只能点 ✕
2. **触觉反馈** —— 勾食材、完成步骤、计时结束时 `navigator.vibrate()`(Android 有效,iOS Safari 不支持,优雅降级)
3. **Tab 切换过渡** —— 现在是硬切
4. **做菜模式左右滑翻步骤**
5. **长列表性能** —— **仅当 P2 实测到掉帧才做**。别提前优化

> `overscroll-behavior: contain` 已在会话 #6 做掉了,不用重复。

### 验收标准

| # | 项 | 标准 |
|---|---|---|
| 1 | 手势 | 抽屉下滑关闭跟手无卡顿;横向滑动不误触发 |
| 2 | 帧率 | 长列表滚动 ≥50fps(真机实测) |
| 3 | 降级 | 所有手势都有等效点按入口,不存在只能用手势完成的操作 |
| 4 | 不回归 | P1 的可点性与溢出验收全部重跑通过 |

---

## P5 · 菜谱库扩充与校对(153 → 200)

**依赖 P4**(已完成)—— 没有校验脚本就扩库,等于一边加菜一边破坏覆盖度约束。

### 具体任务

1. 先跑 `check.js` 看覆盖度短板在哪,**按短板补菜**而不是随便加
2. 补到 200 道,优先:一人食、10 分钟快手、剩菜改造、空气炸锅
3. 高频菜(炒菜 Top 20)的步骤与热量二次校对 —— 尤其**烹调用油有没有算进热量**(同类产品最常见的低估来源)
4. 每批加完立刻跑 `check.js`

### 验收标准

- 200 道 ±5;`check.js` 零 error
- 32 个常用食材仍全部 ≥4 道主料
- 新增菜的 `q` 以 **3 人份**为基准、`nu` 为**每份**(红线第 8 条)
- 随机抽 10 道人工复核步骤可执行性

---

## P9 · 多端:微信小程序(大工程,可选)

### 先澄清一件事:「H5」在 P8 完成后就已经有了

在国内的说法里,H5 基本就等于"能用链接打开的手机网页"。**P8 部署完成的那一刻,H5 版本就存在了**,不需要单独开一个阶段。

托管之后 H5 还能额外做的,只有微信生态的集成:

| 想要 | 需要什么 | 门槛 |
|---|---|---|
| 微信里分享出去带标题和缩略图 | 微信 JS-SDK | **需要已 ICP 备案的域名** + 公众号 |
| 微信内免手动"在浏览器打开" | 无 —— 链接本来就能直接开 | 无 |

所以如果只是想"发微信给朋友点开就能用",**P8 做完就够了,P9 可以不做**。

### 小程序是真正的新工作量

微信小程序**不能跑 DOM**。它有自己的 WXML / WXSS / 运行时,`document.querySelector` 那一套完全不存在。所以视图层必须重写。

好消息是这个项目的结构对移植友好 —— 逻辑和视图分得很干净:

| 部分 | 能否直接移植 | 说明 |
|---|---|---|
| `ING` 食材库(122 项) | ✅ 原样搬 | 纯数据 |
| `RECIPES` 菜谱库(153 道) | ✅ 原样搬 | 纯数据 |
| `match()` 匹配打分算法 | ✅ 原样搬 | 纯函数,零 DOM 依赖 |
| 份数换算 / 购物清单合并 / 库存天数 | ✅ 原样搬 | 纯逻辑 |
| 全部 CSS | ❌ 重写 | WXSS 不支持部分选择器,布局要重来 |
| 所有 `view*()` 渲染函数 | ❌ 重写 | 字符串拼 HTML → WXML 模板 + 数据绑定 |
| 事件委托 / `elementFromPoint` 那套 | ❌ 重写 | 小程序事件模型完全不同 |

**粗估:数据与算法零改动,UI 全部重写,大约等于重做 40%。**

### 两条路(到时候要选)

| 路线 | 做法 | 门槛 |
|---|---|---|
| **A. 原生小程序** | 用 WXML/WXSS 重写视图,复用数据与算法 | 个人主体可注册小程序;**不需要域名备案**;工作量大 |
| **B. `web-view` 套 H5** | 小程序里嵌一个 web-view 加载已部署的 H5,UI 完全不用重写 | **需要 ICP 备案的业务域名** + 小程序类目审核。个人主体基本走不通 |

大概率只能走 **A**。

### ⚠️ 会和现有红线冲突

红线第 1 条是「单文件、零依赖」([ADR-001](DECISIONS.md))。小程序移植需要把数据和算法抽成独立模块给两端共用,这与单文件直接冲突。

**开工前必须新增一条 ADR 来决定:** 是维持单文件(数据在两端各存一份、手工同步,会腐化),还是拆成 `data.js` + 构建步骤把它内联回单文件(破坏零构建)。这是个真实的取舍,不要绕过去。

### 验收标准(到时候细化)

小程序端与 Web 端:同样的食材勾选,推荐结果**完全一致**(说明算法确实是共用的而不是各写一套)。

---

## 随时可做(不占阶段)

- 修 bug(用户报的问题优先于任何阶段任务)
- 加菜谱(遵守红线第 8 条 + 跑 `check.js`)
- 文档订正

---

## 明确不做(范围外)

账号 / 云同步 · 拍照识别食材 · 真实菜品照片 · 菜谱社区 · 买菜电商跳转 · 视频教程 · AI 动态生成菜谱 · **桌面端专属布局**

理由见 [PRD.md](PRD.md) §12 与 [DECISIONS.md](DECISIONS.md)。
