# 今天吃什么 · 产品需求文档 (PRD)

> 本文档定义**要做什么**与**验收标准**。
> 开发节奏与阶段划分见 [ROADMAP.md](ROADMAP.md),当前进度见 [STATE.md](STATE.md),红线见 [CLAUDE.md](CLAUDE.md)。

| 项目 | 内容 |
|---|---|
| 产品名 | 今天吃什么 |
| 版本 | v1.0 |
| 文档状态 | 已确认,开发中 |
| 创建日期 | 2026-08-21 |
| 交付物 | `index.html`(单文件离线 Web App) |
| 相关文档 | [DECISIONS.md](DECISIONS.md) 决策记录 · [WORKLOG.md](WORKLOG.md) 开发日志 |

---

## 1. 背景与问题定义

### 1.1 真实痛点

用户下班回家,站在冰箱前:

> **"我知道家里有什么,但我不知道能做什么。"**

这不是"缺菜谱"的问题 —— 全网菜谱多到过剩。这是一个**反向检索问题**:现有的菜谱产品几乎全都要求用户先想好吃什么菜,再去查怎么做。而真实的家庭场景是**约束先行**:食材是给定的,菜品是未知的。

### 1.2 问题拆解

| 子问题 | 现状 | 后果 |
|---|---|---|
| 不知道现有食材能组合出什么 | 靠记忆和经验,组合空间想不全 | 长期重复做那 5 道菜 |
| 想到了菜但不确定做法 | 现搜,广告多、步骤啰嗦、要跳 App | 中断决策,干脆点外卖 |
| 不知道吃下去多少热量 | 完全没数 | 健康目标无法执行 |
| 差一两样料就做不了 | 全有才敢做,差料就放弃 | 食材烂在冰箱里 |
| 买了忘了吃 | 无临期感知 | 浪费 |

### 1.3 一句话产品定义

**给定家里现有的食材,输出几道现在就能做的菜,附完整做法、热量和缺料提示。**

---

## 2. 竞品调研结论

调研了 8 个同品类产品,分三类:海外食材反查工具、国内菜谱平台、食材保质期管理工具。

### 2.1 同类产品对比

| 产品 | 类型 | 核心机制 | 值得学 | 短板 |
|---|---|---|---|---|
| **SuperCook** | 海外 Web/App | 2000+ 食材分类库勾选,反向匹配百万菜谱;支持"只差 1–2 样"结果 | 食材库分类结构、缺料容忍度、免费无广 | 无中式菜谱、无热量数据 |
| **隔离食用手册 / cook**(云游君,开源) | 国内 Web | 勾选食材 + 厨具 → 组合菜谱,分**模糊 / 严格 / 生存**三档 | **三档匹配模式是本品类最聪明的设计**;厨具作为独立约束维度 | 跳转 B 站视频,站内无步骤、无热量 |
| **今天吃什么 AI 智能食谱** | 国内 App | 输入食材 → LLM 生成菜谱,可追加约束("15 分钟高蛋白") | 约束式二次调整的交互 | 依赖联网 + 订阅;同样输入结果不稳定;热量是模型估的,无法校对 |
| **Pantry Pic** | 海外 App | 拍冰箱照片自动识别食材 | 证实了"输入成本是本品类生死线" | 识别错误率、必须联网 |
| **下厨房** | 国内头部 | 正向搜索为主 | 菜谱内容质量、界面清爽 | 首页被市集/广告占满,用户实际只用搜索功能 |
| **食光冰箱 / 保质记** | 国内工具 | 食材库存 + 保质期提醒 | 临期管理是独立强需求 | **录入成本极高**(要拍照 + 填生产日期 + 填保质期),用户填两次就放弃 |
| **HowToCook**(程序员做饭指南,开源) | GitHub 内容库 | Markdown 菜谱,步骤程序员友好 | 步骤描述的精确性(克数、火候、时间) | 纯内容无产品;无热量;无结构化数据 |
| **Frigo Magic** | 海外 App | 减少浪费导向的菜谱生成 | "剩料优先"的价值主张 | 西式菜谱为主 |

### 2.2 提炼出的 7 条设计原则

这 7 条直接构成本产品的设计约束:

1. **输入成本是生死线。** Pantry Pic 的产品结论是"一旦去掉打字这道苦力,后面整个流程的使用率暴涨"。
   → **本产品:食材选择必须是分类网格 + 一键勾选 + 高频食材置顶,绝不做纯输入框。**

2. **匹配不能是布尔运算。** 严格 AND 匹配会让绝大多数组合返回空结果,而空结果是本品类最大的体验杀手。
   → **本产品:匹配度打分 + 三档分级(现在就能做 / 差一两样 / 凑一凑也行),永不返回空。**

3. **缺料要正面展示,不要隐藏。** "差 1 样:生姜"远比藏掉这道菜有用。
   → **本产品:缺料清单 + 替代方案 + 一键进购物清单。**

4. **下厨房的教训:不要把首页让给非核心内容。** 用户最终只用搜索功能。
   → **本产品:首页只做一件事 —— 选食材 → 出菜。无广告、无社区、无商城。**

5. **做菜模式必须独立。** 灶台前的手是湿的、油的,眼睛离屏幕一米远。
   → **本产品:全屏做菜模式,单步聚焦、超大字号、步骤打勾、内置计时器、屏幕不休眠、无弹窗。**

6. **热量要"一眼可见 + 可展开"。** 顶部一条概览(时长/份数/热量/难度),展开看蛋白脂肪碳水;份数可调且数据联动。

7. **临期管理的通病是录入成本。** 竞品要求填生产日期 + 保质期。
   → **本产品:只问"还能放几天",六个快捷按钮(1天 / 3天 / 1周 / 2周 / 1个月 / 长期),两秒录完。**

---

## 3. 目标用户与场景

### 3.1 目标用户

**主用户画像:** 自己在家做饭的上班族,会做基础家常菜(能炒能炖),家里常备 10–30 种食材,做饭对象是 3–4 口人的家庭。

不是厨房小白(不需要教怎么切菜),也不是美食爱好者(不追求复刻餐厅菜)。

### 3.2 核心场景

| # | 场景 | 触发 | 期望结果 | 时间预算 |
|---|---|---|---|---|
| S1 | **下班回家决策** | 站在冰箱前 | 30 秒内看到 3–5 道现在能做的菜 | < 30s |
| S2 | **完全不想选** | 决策疲劳 | 一键随机给一套方案,直接开做 | < 5s |
| S3 | **照着做** | 已选定菜 | 步骤清晰、免手动、带计时 | 全程 |
| S4 | **清临期** | 想起有菜要坏了 | 优先推荐消耗临期食材的菜 | < 30s |
| S5 | **健康执行** | 在减脂/增肌期 | 只看符合热量目标的菜 | < 30s |
| S6 | **顺路买菜** | 在超市 / 出门前 | 差什么料一目了然,可复制清单 | < 1min |
| S7 | **规划一周** | 周末采购前 | 一周菜单 + 合并采购清单 | < 3min |

### 3.3 明确不服务的场景

- 不做菜谱社区(不发布、不评论、不关注)
- 不做外卖 / 买菜电商跳转
- 不做多设备云同步(单机 localStorage)
- 不教基础刀工和厨房入门

---

## 4. 用户故事与验收标准

### Epic A · 食材选择与推荐(核心闭环)

**A1** 作为用户,我要**按分类勾选家里现有的食材**,这样我不用打字。
- 食材按 7 个分类分组:常用 / 蔬菜 / 肉禽蛋 / 水产 / 豆制品与奶 / 主食 / 干货菌菇
- "常用"区置顶,放最高频食材(鸡蛋、土豆、西红柿、猪肉…)
- 单击勾选 / 取消,已选食材在顶部形成可删除的 chip 条并显示计数
- 支持搜索框按**中文名 / 别名 / 拼音**过滤(番茄 → 西红柿;tudou → 土豆),打分排序:中文名前缀 > 中文名包含 > 拼音前缀 > 别名 > 拼音中段
- **搜索框必须留在静态区,输入时只重绘结果区。** 中文输入法靠 composition 组字会话工作,重建 `<input>` 会打断组字,导致打不出中文 —— 见 [ADR-020](DECISIONS.md)
- 勾选食材、切分类、删 chip 均为局部刷新,**不整页重绘** —— 否则手机键盘会在每次点选后收起
- 选择状态自动持久化,下次打开还在

**A2** 作为用户,我要看到**基于所选食材推荐的菜**,并知道每道菜的匹配程度。
- 结果分三档展示,每档有标题和说明:**① 现在就能做 / ② 差一两样 / ③ 凑一凑也行**
- 每张菜卡显示:emoji 视觉块、菜名、分类标签、总时长、难度、每份热量、匹配状态
- "差一两样"档的卡片直接在卡上写出缺什么(如 `差 1 样:生姜`)
- **永不出现空结果**:若三档全空,给出引导并推荐仅需基础调味的菜
- 结果可按 匹配度 / 热量升序 / 时长升序 / 蛋白质降序 排序

**A3** 作为用户,我要看到**一道菜的完整做法**,包含所有食材用量和分步骤操作。
- 概览条:总时长 · 份数 · 每份热量 · 难度 · 所需厨具
- 食材表分**主料 / 辅料 / 调味**三段,每项带用量和单位;缺的料标红并给替代建议
- 步骤编号列出,带该步骤预计耗时;含火候、时间等可执行细节
- 营养展开区:蛋白质 / 脂肪 / 碳水 克数 + 占比条
- 小贴士(1 条关键经验)

**A4** 作为用户,我要**调整份数**,所有用量和热量跟着变。
- 份数选择 1 / 2 / 3 / 4 / 6 人,默认 3 人
- 食材用量按比例实时重算并合理取整
- 每份热量保持不变,合计热量 = 每份 × 份数,两个数都显示
- 购物清单数量随之联动

### Epic B · 我的厨房(库存与临期)

**B1** 作为用户,我要**长期记录家里有什么**,不用每次重新勾。
- 库存与"本次勾选"是两个独立概念:库存是长期记录,可一键"全部带入本次选择"
- 库存项显示剩余天数和状态色

**B2** 作为用户,录入库存必须**两秒内完成**。
- 点食材 → 弹出六个快捷按钮:1天 / 3天 / 1周 / 2周 / 1个月 / 长期
- 不出现任何日期选择器,不要求填生产日期或保质期
- 默认值按食材类型智能预填(叶菜 3 天、肉类 3 天、根茎 2 周、干货长期)

**B3** 作为用户,我要**优先消耗快坏的食材**。
- 剩余 ≤ 2 天标"临期"(橙),≤ 0 标"已过期"(红)
- 推荐排序中,命中临期食材的菜显著加权上浮
- 菜卡上标出"可消耗临期:菠菜"

### Epic C · 忌口 / 厨具 / 营养目标(过滤器)

**C1** 作为用户,我要设一次忌口就永久生效。
- 忌口食材多选(香菜、内脏、羊肉、葱、姜、蒜、辣椒…)+ 自定义
- 素食模式:排除所有含肉禽水产主料的菜
- 清真模式:排除含猪肉及料酒的菜
- 辣度上限 0–3 档
- 命中忌口的菜是**硬排除**,不出现在任何档位

**C2** 作为用户,我要按**厨具**过滤(轻量设置项,不占主流程)。
- 设置页勾选拥有的厨具:炒锅 / 汤锅 / 电饭锅 / 微波炉 / 烤箱 / 空气炸锅 / 高压锅 / 蒸锅
- 缺少必需厨具的菜默认隐藏,可一键"忽略厨具限制"
- 炒锅 + 汤锅 + 蒸锅默认已勾选

**C3** 作为用户,我要按**营养目标**筛选。
- 四种目标:均衡 / 减脂 / 增肌 / 控糖
- 减脂:每份 ≤ 450 kcal 优先,按"蛋白热量占比"加权
- 增肌:按蛋白质绝对量加权,每份蛋白 ≥ 20g 额外加分
- 控糖:每份碳水 ≤ 20g 加权,降权高碳水主食
- 可选硬上限:"每份不超过 ___ kcal"

### Epic D · 缺料、替代与购物清单

**D1** 作为用户,差料的时候我要知道**能不能替代**。
- 菜谱内置常见替代关系(老抽 → 生抽 + 糖;蚝油 → 生抽 + 糖;香醋 → 白醋 + 糖…)
- 缺料项直接展示替代方案文本

**D2** 作为用户,我要把**多道菜缺的料合并成一张采购清单**。
- 菜卡和详情页可"加入今天做"
- 清单页自动合并去重:同食材同单位累加数量
- 按食材分类分组,可勾选打勾
- 一键复制为纯文本(便于发微信 / 记事本)
- 显示"今天做"这几道菜的合计热量

### Epic E · 做菜模式

**E1** 作为用户,做菜时我要**免手动、看得清**。
- 全屏模式,单步聚焦,字号 ≥ 22px
- 顶部进度条 + "第 3 / 7 步"
- 大面积的"上一步 / 下一步"按钮(拇指可达)
- 带时长的步骤有 ⏱ 按钮,一键启动倒计时;倒计时结束有提示音 + 标题闪烁
- 尝试请求 Wake Lock 保持屏幕常亮,不支持则静默降级
- 完成时自动记入历史

### Epic F · 摆一摆与配餐

**F1** 作为用户,我实在不想选的时候要**一键出结果**。
- "摆一摆"按钮:从候选池里随机抽一道,带翻牌动画
- **候选池 = 全部「现在就能做」+ 按分数从高到低补「差一两样」,补到至少 `ROLL_MIN`(12)道。**
  只取「现在就能做」的话池子常常只有 3 道(tier 0 要求主料+辅料+调味一样不缺,门槛极高),
  骰子会在那 3 道里来回打转,用户看起来就是坏了。理由与实测见 [ADR-023](DECISIONS.md)
- **补进来的是「按分数补到够」,不是「整档吞下」。** 整档吞下会让 90% 的结果都要先买东西,
  骰子就从「今天做什么」变成了「明天买什么」
- **摆出「差一两样」的菜时,卡片必须写出差什么**,措辞与菜卡一致:`差 N 样:xxx、yyy`
- 连续摆不重复(直到候选耗尽)

**F2** 作为用户,我要**一日配餐**。
- 一键生成 1 荤 + 1 素 + 1 汤/主食 的搭配,显示合计热量
- 可对单道菜"换一个"而不重摆整套

**F3** 作为用户,我要**一周菜单**。
- 生成 7 天 × 每天(1 荤 + 1 素),同一道菜一周内不重复
- 一键把整周缺料合并进购物清单

### Epic G · 收藏与历史

- **G1** 收藏常做菜,收藏的菜在推荐中小幅加权
- **G2** 记录做过的菜和日期;7 天内做过的显著降权,30 天内小幅降权,避免天天推同一道
- **G3** 历史页可查看最近做过什么,可清空

---

## 5. 信息架构

```
底部 Tab(移动端) / 顶部 Tab(桌面端)
├── 🧺 选食材      分类网格勾选 + 搜索 + 已选 chip 条 + 「看看能做什么」主按钮
├── 🍽️ 今天吃      三档推荐结果 + 排序 + 摆一摆 + 配餐入口
│     └── 菜谱详情(移动端底部抽屉 / 桌面居中弹层)
│           └── 做菜模式(全屏)
├── 🧊 我的厨房    库存列表(按剩余天数排序)+ 快捷录入 + 临期提醒条
├── 🛒 买菜清单    今天做的菜 + 合并缺料清单 + 复制
└── ⚙️ 我的        份数 / 营养目标 / 忌口 / 厨具 / 调味基础包 / 收藏 / 历史 / 数据管理
```

---

## 6. 核心算法规格:菜谱匹配

### 6.1 输入

| 符号 | 含义 |
|---|---|
| `S` | 用户本次选中的食材集合(勾选 + 可选带入的库存) |
| `P` | 调味基础包 · 21 样(默认视为家里都有:盐、食用油、糖、生抽、老抽、醋、料酒、葱、姜、蒜、淀粉、胡椒粉、蚝油、鸡精、香油、干辣椒、冰糖、八角、花椒、白芝麻、黑胡椒) |
| `X` | 忌口食材集合 |
| `T` | 拥有的厨具集合 |
| `G` | 营养目标 |

### 6.2 硬过滤(不满足直接排除)

1. 菜谱任意食材 ∈ `X` → 排除
2. 素食模式且菜谱含肉禽水产主料 → 排除
3. 清真模式且菜谱含猪肉或料酒 → 排除
4. 菜谱辣度 > 辣度上限 → 排除
5. 菜谱必需厨具 ⊄ `T`(且未开启"忽略厨具") → 排除
6. 设了 kcal 硬上限且每份热量超限 → 排除

### 6.3 缺料计算

```
missMain   = 主料中不在 S 的项
missSub    = 辅料中不在 S 的项
missSeason = 调味中既不在 S 也不在 P 的项(调味包开启时通常为 0)
missTotal  = |missMain| + |missSub| + |missSeason|
```

### 6.4 分档

| 档位 | 条件 | 文案 |
|---|---|---|
| **Tier 0** | `missTotal == 0` | 现在就能做 |
| **Tier 1** | `missTotal ≤ 2` 且 `|missMain| ≤ 1` | 差一两样 |
| **Tier 2** | 主料命中率 ≥ 50% | 凑一凑也行 |
| 排除 | 以上都不满足 | — |

> **设计意图:** Tier 1 的双重条件很关键 —— 允许差 2 样辅料/调味,但最多只允许差 1 样主料。差 2 样主料的菜本质上已经是另一道菜了,放进"差一两样"会让用户失去信任。

### 6.5 打分(档内排序)

```
score = 100
      + 40 × 主料命中率
      + 15 × 辅料命中率
      + 25 × 食材利用率            (用掉了多少比例的已选食材,鼓励清冰箱)
      +  8 × 每个命中的临期食材      ← 差异化加权
      + 15  若已收藏
      + 营养目标专项加权            (见 C3)
      -  6 × missTotal
      - 30  若 7 天内做过
      - 10  若 8–30 天内做过
      -  5  若时长 > 60 分钟        (工作日时间成本)
```

排序:`tier` 升序 → `score` 降序。

### 6.6 空结果保护与零输入保护

- **零输入保护(必须):** 若 `S` 为空(一样都没勾),`match()` 直接返回空列表,交给引导页。
  不做这个短路的话,所有「单主料 + 全基础包调味」的菜会因为 `missMain=1 / missTotal=1`
  恰好落进 Tier 1,显示成"差 1 样:南瓜"—— 用户什么都没选,说"差一样"是误导。
- 若 Tier 0 + Tier 1 均为空 → 自动展开 Tier 2 并给出提示
- 若三档全空(有输入但匹配不上) → 展示引导页 + 推荐"仅需基础调味 + 1 样主料"的菜(如蛋炒饭、阳春面)

---

## 7. 数据模型

### 7.1 食材

```js
{
  id: 'wuhuarou',       // 唯一 id(拼音)
  name: '五花肉',
  cat: 'meat',          // veg | meat | fish | soy | staple | dry | season
  emoji: '🥓',
  hot: true,            // 是否进「常用」置顶区
  aliases: ['三层肉','腩肉'],
  shelfDays: 3,         // 库存录入的智能默认值
  animal: 'pork'        // 素食/清真判定:null | pork | meat | fish
}
```

### 7.2 菜谱

```js
{
  id: 'hongshaorou',
  name: '红烧肉',
  emoji: '🍖',
  cat: 'stew',                    // stirfry|stew|soup|cold|steam|staple|breakfast|bake
  cuisine: '家常',
  time: 75,                       // 分钟(总时长)
  diff: 3,                        // 1–5
  spicy: 0,                       // 0–3
  tools: ['pot'],                 // 必需厨具 id
  tags: ['下饭','宴客'],
  main:   [{ ing:'wuhuarou', qty:500, unit:'g' }],   // 参与匹配,权重高
  sub:    [{ ing:'jiang',    qty:3,   unit:'片' }],   // 参与匹配,权重低
  season: ['bingtang','shengchou','laochou','liaojiu','yan'],  // 调味包覆盖
  nutr:   { kcal:520, p:22, f:42, c:9 },             // 每份(3 人份基准)
  steps:  [{ t:'五花肉切 3cm 见方…', min:5 }],
  tips:   '…',
  subs:   { laochou:'可用生抽 + 半勺糖上色' }
}
```

**用量与营养的换算约定:**
- `qty` 以 **3 人份** 为基准存储
- 显示用量 = `qty × 份数 / 3`,按单位智能取整(g 取整到 5,个/片/勺保留 0.5)
- `nutr` 是**每份**数值,不随份数变化;合计 = `nutr × 份数`

### 7.3 本地存储(localStorage · key `eatwhat_v1`)

```js
{
  selected:  ['jidan','tudou'],                        // 本次勾选
  inventory: [{ ing:'bocai', addedAt:1755..., days:3 }],
  prefs: {
    servings: 3,
    goal: 'balance',                                    // balance|cut|bulk|lowcarb
    kcalCap: null,
    avoid: ['xiangcai'],
    vegetarian: false, halal: false, maxSpicy: 3,
    tools: ['wok','pot','steamer'],
    staples: true,                                      // 调味基础包
    ignoreTools: false
  },
  favs:    ['hongshaorou'],
  history: [{ id:'hongshaorou', at:1755... }],
  today:   ['hongshaorou','paihuanggua'],               // 今天做(购物清单来源)
  weekPlan: null
}
```

---

## 8. 视觉设计规范

**风格:暖米色杂志风 / 纸质食谱书**

> **唯一事实来源是 [index.html](index.html) 的 `:root{}` 块**(17 个 token,全部只定义一次、无别处覆盖)。
> 下表照它逐条校对过(2026-08-25)。改 token 必须同步改这张表 —— [CLAUDE.md](CLAUDE.md) 红线第 7 条。

**颜色(13 个)**

| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#F5F0E8` | 页面底色 |
| `--surface` | `#FFFDF8` | 卡片 |
| `--surface-2` | `#EDE4D3` | 次级面、标签底 |
| `--ink` | `#2B2015` | 正文 |
| `--ink-2` | `#7A6A56` | 次级文字 |
| `--ink-3` | `#A2937E` | **三级文字** —— 分档标题旁的道数、说明小字、各处 muted 文本 |
| `--primary` | `#8B4513` | 赭石 · 标题、主按钮 |
| `--primary-soft` | `#A9673A` | 悬停、次级强调 |
| `--warn` | `#C2703D` | **橙色强调** —— 临期徽标、**缺料提示**、辣度标签、收藏星、提示条边框。**不只是"临期"** |
| `--danger` | `#A63A2E` | 过期红、忌口 |
| `--ok` | `#5C7A4A` | 健康绿、可做、能消耗临期 |
| `--line` | `#DCCFB8` | 1px 分隔线(默认) |
| `--line-2` | `#C9B99C` | **二级分隔线(深一档)** —— 卡片外框、分档标题下划线、缺料行的虚线上边框 |

**字体(2 个)**

| Token | 值 |
|---|---|
| `--serif` | `'Noto Serif SC','Source Han Serif SC','Songti SC','STSong','SimSun',serif` |
| `--sans` | `-apple-system,BlinkMacSystemFont,'PingFang SC','Microsoft YaHei','Hiragino Sans GB','Helvetica Neue',Arial,sans-serif` |

标题用 `--serif`,正文用 `--sans`。`--sans` 打头的 `-apple-system` / `BlinkMacSystemFont` 是给 iOS / Chrome
走系统字的,**不能删** —— 删了 iOS 上会掉到 PingFang 之外的回退字。

**布局(2 个)**

| Token | 值 | 用途 |
|---|---|---|
| `--tabh` | `58px` | 底部 Tab 栏高度。吸底 CTA 与列表底部留白都靠它算,不要写死 |
| `--frame` | `480px` | 手机画框宽度上限(见下方「目标平台与布局」) |

**不是 token 但属于规范的一个值**

`body{background:#E4DBC9}` —— 画框外的底色,比 `--bg` 深一档。手机上被 `#app` 完全盖住,只在桌面浏览器里露出。
- **无阴影**:层级只靠 1px 实线、底色差和留白表达
- **圆角**:卡片 4px(纸感,不用大圆角)
- **纸纹**:极淡的 CSS 渐变噪点,不超过 2% 对比度
- **菜品视觉块**:emoji(48–72px)居中于按菜系生成的双色柔和渐变块

### 目标平台与布局:**只做手机端**

**只维护一套移动布局。** 2026-08-22 方向收窄,不再做桌面自适应 —— 见 [ADR-019](DECISIONS.md)。

| 环境 | 布局 |
|---|---|
| **手机(320–500px,主场景)** | 单列;底部 5 项 Tab 栏;详情为底部抽屉;`viewport-fit=cover` + `env(safe-area-inset-*)` 让开刘海与小白条 |
| **桌面浏览器(调试与偶尔查看)** | 内容收进居中的 **480px 手机画框**,画框外底色压暗一档 + 1px 界线。**布局与手机完全一致**,不是另一套设计 |

**允许存在的媒体查询**(都属于移动端内部适配,不构成第二套布局):

| 断点 | 作用 |
|---|---|
| `max-width: 400px` / `344px` | 小屏手机标题字号收缩 |
| `min-width: 700px` | 做菜模式步骤字号放大(服务大屏手机横屏) |

**禁止:** 任何 `min-width: 640px` / `1024px` 之类的桌面断点。这是 [CLAUDE.md](CLAUDE.md) 红线第 2 条。

**画框对齐要求:** `.tabbar` / `.stickycta` / `.sheet` 都是 `position:fixed`,必须与画框左右边界对齐(误差 ≤1px),不能横跨整个桌面视口。`.scrim` 遮罩例外,保持全屏。

---

## 9. 非功能需求

| 项 | 要求 |
|---|---|
| 离线 | 完全离线可用,无任何网络请求(含字体、图片) |
| 部署 | 双击 `index.html` 即可运行,`file://` 协议下功能完整 |
| 依赖 | 零外部依赖,不使用 ES Module(`file://` 不支持) |
| 性能 | 首屏 < 300ms;153 道菜的全量匹配计算 < 16ms(单帧内,每次交互都全量重算) |
| 目标平台 | **只做手机端**(320–500px)。桌面浏览器下用 480px 居中画框呈现同一套布局,见 §8 与 [ADR-019](DECISIONS.md) |
| 兼容 | iOS Safari 与 Android Chrome 为**必测主目标**;桌面 Chrome / Edge 仅需画框可用(开发调试用) |
| 数据 | 全部本地;提供导出 / 导入 JSON 和清空 |
| 无障碍 | 正文对比度 ≥ 4.5:1;点击目标分三级(见下) |
| iOS 适配 | 13 项既定动作,见 [ADR-021](DECISIONS.md)。硬性两条:**所有输入框 ≥16px**(否则 iOS 聚焦时强制放大整页)、**高度用 `dvh` 不用 `vh`**(iOS 的 `vh` 含地址栏) |

**点击目标尺寸分级**(实测口径,见 [DECISIONS ADR-012](DECISIONS.md)):

| 层级 | 最小高度 | 适用 |
|---|---|---|
| 主控件 | **44px** | 主按钮、底部 Tab、搜索框、食材格、分段控件、图标按钮 |
| 行内次级动作 | **40px** | 列表行里的「重设/删除」图标按钮 |
| 行内文字按钮 | **38px** | 卡片/行内的「看」「换」「清空」等小按钮 |


---

## 10. 内容规格:菜谱库

**实际总量:153 道**(原目标 120 道 → 因覆盖度审计扩充,见 [DECISIONS ADR-015](DECISIONS.md))

| 分类 | 目标 | 实际 | 说明 |
|---|---|---|---|
| 炒菜 | 38 | **52** | 家常主力 |
| 炖菜 / 焖菜 | 18 | **22** | 红烧、炖、焖 |
| 汤羹 | 16 | **19** | 清汤、浓汤、甜羹 |
| 凉菜 | 12 | **19** | 拌、白灼 |
| 蒸菜 | 10 | **10** | 蒸鱼、蒸蛋、蒸肉 |
| 主食 / 面点 | 16 | **17** | 面、饭、饼、粥 |
| 早餐 | 6 | **9** | 快手早餐 |
| 烤箱 / 空气炸锅 | 4 | **5** | 需要对应厨具 |
| **合计** | 120 | **153** | |

**内容体量:** 848 个操作步骤(平均 5.5 步/道),每步带预计耗时;每道菜含 1 条关键经验提示。

**食材库:** 122 项 —— 蔬菜 39 / 肉禽蛋 14 / 水产 7 / 豆制品奶 8 / 主食 9 / 干货菌菇 13 / 调味 32(其中基础包 21 + 非基础包 11)。其中 32 项标为"常用"进入置顶区。

**厨具依赖:** 默认勾选的炒锅 + 汤锅 + 蒸锅可覆盖 **148 / 153 道**;剩余 5 道需烤箱(3)或空气炸锅(2),默认隐藏并在结果页底部提示可一键放开。

**食材覆盖要求(已达成并持续校验):** 每一个进入"常用"区的高频食材,至少出现在 **4 道菜的主料**中;且不存在任何"勾了却出不来菜"的食材。此项由构建期校验脚本强制检查。

**热量范围:** 47 – 610 kcal/份。

**热量数据的诚实声明:** 营养数据基于常用食材成分表按配方用量估算,在产品内标注为**估算值**,误差约 ±15%。不伪装为实验室测定值。校验脚本额外强制"标注热量"与"按蛋白/脂肪/碳水推算热量"的偏差不超过 22%,防止数据内部自相矛盾。

---

## 11. 验收标准(v1.0 Definition of Done)

**验收方式:** 全部在真实浏览器里实测。功能项通过在页面上下文执行断言脚本驱动真实 DOM 与状态;布局项通过 375px iframe 画框实测(headless 浏览器视口下限为 500px,直接设 375 拿不到真实视口,必须用 iframe 约束)。

### 功能

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 双击 `index.html` 运行,控制台无报错,数据自检通过 | ✅ 153 道菜 / 122 项食材,零未知引用、零重复 id |
| 2 | 只勾「鸡蛋 + 西红柿」→ Tier 0 至少 3 道 | ✅ 3 / 44 / 4 |
| 3 | 只勾「鸡蛋」→ 结果非空 | ✅ 1 / 43 / 4 |
| 4 | 一样都不勾 → 走引导,不白屏不报错 | ✅ 三档全空 + 引导页 + 6 张「只要一样就能做」兜底卡 |
| 5 | 勾 15 样常见食材 → 三档合计 ≥ 20 | ✅ 15 / 70 / 11,合计 96 |
| 6 | 忌口香菜 → 所有含香菜的菜从三档消失 | ✅ 硬排除生效(95 → 89) |
| 7 | 素食模式 → 无任何含肉禽水产的菜(蛋奶保留) | ✅ 肉类 0 道,含蛋主料 16 道仍在 |
| 8 | 清真模式 → 无猪肉/料酒的菜 | ✅ 0 道 |
| 9 | 辣度上限 0 → 无辣菜 | ✅ 全部 sp=0 |
| 10 | 每份热量硬上限 200 → 全部 ≤ 200 | ✅ 剩 40 道全部达标 |
| 11 | 未勾烤箱 → 烤箱菜隐藏并上报数量 | ✅ 隐藏 5 道,提示可一键放开;勾上后烤箱 3 道 / 炸锅 2 道出现 |
| 12 | 份数 3 → 6:用量翻倍、每份热量不变、合计翻倍 | ✅ 600g→1200g;540=540;1620→3240 |
| 13 | 库存加「剩 1 天」→ 相关菜上浮并标注可消耗临期 | ✅ 升至首位,菜卡显示「✓ 能消耗临期:菠菜」 |
| 14 | 做菜模式:步骤可翻、计时器可用、字号足够、退出记入历史 | ✅ 字号 25px,计时器启停正常,完成写入历史 |
| 15 | 做过的菜被降权 | ✅ 刚做过的菜退出同档首位 |
| 16 | 加多道菜 → 清单正确合并同类食材,可复制 | ✅ 青椒 3+2=5 个;五花肉 600+400=1000g;文本按分类分组 |
| 17 | 摆一摆连续摆不重复 | ✅ 20 次零连续重复(候选耗尽时保留当前项再重置) |
| 17b | 摆一摆候选池够大 | ✅ 2026-08-25 修:40 次点击摆出 12 道不同的菜(修前只有 3 道来回换)。见 [ADR-023](DECISIONS.md) |
| 17c | 摆到「差一两样」时标出缺料 | ✅ 卡片显示「差 2 样:面条、小白菜」,警示色与菜卡一致 |
| 18 | 一周菜单 14 道无重复 | ✅ 14 道去重后仍 14,每天一荤一素 |
| 19 | 一日配餐 1 荤 + 1 素 + 1 汤/主食,可单换 | ✅ 第三格恒为 soup/staple,单换不重摆整套 |
| 20 | 刷新后选择、库存、偏好、收藏、历史、清单全部保留 | ✅ 序列化往返完整;损坏数据被 try/catch 兜住不炸应用 |
| 21 | 关掉调味基础包 → 进入严格模式 | ✅ Tier 0 从 3 道降到 0 道 |

### 可点性(必测 · 见 [ADR-018](DECISIONS.md))

> **这一节是因为漏测导致过整页点击失效才补上的,不能省。**
> 用 `element.click()` 做的功能测试只能验证"点到之后会发生什么",**验证不了"点不点得到"** ——
> 它绕过浏览器命中测试。必须另外用 `document.elementFromPoint()` 实测。

| 检查项 | 方法 | 结果 |
|---|---|---|
| 无不可见的点击拦截物 | 扫出所有 `fixed`/`absolute`、覆盖视口 ≥25%、`opacity < 0.05` 却仍 `pointer-events:auto` 的元素 | ✅ 6 个非模态视图全部为 0 |
| 控件真实可达 | 对每个控件取中心点,`elementFromPoint` 返回的最顶层元素须是它本身或其后代 | ✅ 选食材/今天吃/我的厨房/买菜清单/我的/配餐:Tab 栏、主按钮、食材格、分段控件、图标按钮、chip、分类栏、清单项、开关 **全部可点,零拦截** |
| 模态自身控件可达 | 抽屉与做菜模式打开时,测其内部控件(需禁用 CSS 过渡以排除时序干扰) | ✅ 抽屉:3 个底部按钮 + 关闭 + 5 个份数按钮全部可点;做菜模式:上一步/下一步/关闭全部可点 |
| 模态确实屏蔽背景 | 模态打开时,背景控件应全部被拦 | ✅ 抽屉由 `#scrim` 屏蔽,做菜模式由 `.cook` 屏蔽 |
| 桌面断点无隐藏拦截物 | 1280 / 1440px 下复测(该断点 `.sheet` 为居中定位 + `opacity:0`) | ✅ `#sheet pointer-events=none`,全部控件可点 |

**已知且属预期的"被拦":**
- 压在底部固定 Tab 栏下的菜卡 —— 滚动即可,`main` 有 `padding-bottom` 保证最后一张能露出
- 抽屉里被吸底操作栏盖住的食材信息行 —— 非可点元素,且可滚动
- 抽屉关闭时其内部的关闭按钮 —— 桌面断点下居中但透明且 `pointer-events:none`,正是应有行为

### 布局(实测 375px)

| 视图 | 横向溢出 | 页面级溢出元素 | 偏小控件 |
|---|---|---|---|
| 选食材 / 今天吃 / 我的厨房 / 买菜清单 / 我的 / 菜谱详情 / 配餐 | 全部 **0px** | **0** | **0** |

- 主控件实测:主按钮 44px、底部 Tab 57px、搜索框 44px、食材格 66px、分段控件 44px、图标按钮 44px
- `catnav` 分类栏内有元素超出右边界属**预期**(该栏为横向滚动容器),不构成页面级溢出

> **⚠️ 本表数据来自 v1.0(桌面自适应版本),P1 纯手机化改造后必须重测。**
> 改造后的验收口径升级为 **6 个手机宽度** ——`320 / 360 / 375 / 390 / 414 / 480px`,每个宽度下 7 个视图均需零溢出;
> 另加桌面 `1280 / 1440px` 下的**画框对齐**检查(`.tabbar` / `.stickycta` / `.sheet` 与 `#app` 边界误差 ≤1px)。
> 完整验收清单见 [ROADMAP.md](ROADMAP.md) P1。原「桌面端三列菜卡 / 顶部 Tab 栏」一项已随桌面布局一并废除。

### 视觉规范一致性

| 项 | 结果 |
|---|---|
| 页面底色 `#F5F0E8` | ✅ rgb(245,240,232) |
| 主色赭石 `#8B4513` | ✅ rgb(139,69,19) |
| 标题衬线字体栈 | ✅ Noto Serif SC → Songti SC → SimSun → serif |
| 无阴影约束 | ✅ 卡片 / Tab 栏 box-shadow 均为 none |
| 菜品分类渐变色块 | ✅ 8 个分类各自渐变生效 |

## 12. 范围外(v1.0 不做)

| 不做 | 原因 |
|---|---|
| 账号 / 云同步 | 单机场景已满足;引入后端违背"双击即用" |
| 拍照识别食材 | 需联网 + 模型,离线约束下不可行 |
| 真实菜品照片 | 版权风险 + 单文件体积;用 emoji + 渐变块替代 |
| 菜谱社区 / UGC | 竞品的失败教训;偏离核心问题 |
| 买菜电商跳转 | 变商业化产品,污染核心体验 |
| 视频教程 | 单文件离线不可行 |
| AI 动态生成菜谱 | 已明确选择离线内置库,保证结果稳定可校对 |

---

## 13. 后续可能的迭代方向(记录,不承诺)

- 菜谱库扩至 200+,增加菜系细分标签
- 食材用量的"目测换算"提示(一把 / 一小碗 ≈ 多少克)
- 导出一周菜单为图片
- 可选接入 Claude API 做"换点新花样"的补充生成(降级为可选增强,不改变离线主路径)
- PWA 化,支持添加到主屏幕
