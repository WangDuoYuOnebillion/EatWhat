# 开发日志 (Worklog)

按时间倒序记录。每次有实质变动就追加一条,包含:做了什么、为什么、影响到哪些文件、遗留问题。

相关文档:[CLAUDE.md](CLAUDE.md) · [STATE.md](STATE.md) · [ROADMAP.md](ROADMAP.md) · [PRD.md](PRD.md) · [DECISIONS.md](DECISIONS.md)

---

## 2026-08-25

### #017 · 修 bug:摆一摆只在三道菜之间打转(会话 #8,插在 P2 之前)

**做了什么**

用户在准备 P2 真机走查时报告:右上角 🎲 连点多次只有三道菜来回换。定位 + 修掉,三处改动:

1. **`doRoll()` 的候选池改成"补到够"。** 原来是 tier 0 一档,`if(!pool.length)` 才补下一档 ——
   而 tier 0 要求主料+辅料+调味一样不缺,实测勾 6 样食材时只有 3 道。
   现在:tier 0 全要,不够 `ROLL_MIN`(12)就按分数补 tier 1 / tier 2,**补到够就停**。
2. **摆一摆卡片加缺料行。** 从 `mm.list` 里取整条(而非 `recOf()` 只拿菜谱),
   有缺料就渲染 `差 N 样:xxx`,措辞和菜卡一致。
3. **新增 `.roll .miss` CSS。** 原有 `.miss` 只写在 `.dish` 下,够不到骰子卡片。

**为什么(以及为什么不是设计取舍)**

同一份文件里三个同类功能,补候选的写法不一致:`openWeek()` 是 `pool.length<6`、
`doDayPlan()` 是 `pool.length<3`,只有 `doRoll()` 写成 `!pool.length`(全空才补)。
同样的意图三种写法,骰子那种恰好效果最差 —— tier0 只要有 1 道就永远锁死在那 1 道。
详见 [ADR-023](DECISIONS.md)。

**踩到的坑:第一版改法反而更糟**

先写成"不够就把整档 tier 1 放进来"。语法过、池子从 3 涨到 51,看着像成功了。
但一量结果:**40 次点击里只有 3 次是完全够料的**,其余都要先买东西。
骰子从"今天做什么"变成了"明天买什么" —— 池子大小对了,产品意义反而坏了。
改成"按分数补到 12 道"之后:6 样食材 → 9/40 够料,**16 样食材 → 30/40 够料**。
勾得越多骰子越贴近"现在就能做",这才是对的退化曲线。

**教训:池子大小不是目标,结果的可用性才是。** 只看"不同菜数从 3 涨到 40"会误判成修好了。

**顺带证伪了一个听起来更安全的方案**

用户可选的方案里有"只补 `missMain===0` 的菜"(缺的只是辅料,更保险)。实测否掉:
6 样食材下 tier1 的 48 道里 **47 道都缺 1 个主料,只有 1 道是纯缺辅料** ——
因为葱姜蒜都在调味基础包里,本来不算缺。那个方案只会让池子从 3 道变 4 道。

**实测(本地 http server + 真浏览器 375×812,派发真实事件序列)**

| 项 | 结果 |
|---|---|
| 40 次点击摆出不同菜数(6 样) | 3 → **12** |
| 完全够料比例(6 样 / 16 样) | 9/40 / **30/40** |
| tier 0 的菜误显示缺料行 | 否 ✅ |
| tier 1 的菜显示缺料行 | ✅「差 2 样:面条、小白菜」,警示色与菜卡一致(同为 `rgb(194,112,61)`) |
| 零勾选点骰子 | 无异常,toast + 跳回选食材 ✅ |
| `#btnRoll` / `#rollAgain` / 底部 5 Tab | `elementFromPoint` 全部可点 ✅(红线 4) |
| 缺料行是否溢出卡片 / 视口 | 都不溢出 ✅ |
| `check.js` | 0 error(3 条营养 warning 是既有设计带内) |
| `<script>` 块语法 | 通过 |

**影响文件**

`index.html`(`ROLL_MIN` + `doRoll()` + `viewDish()` 摆一摆卡片 + `.roll .miss` 样式)·
`PRD.md`(F1 规格 + 验收表 17b/17c)· `DECISIONS.md`([ADR-023](DECISIONS.md))

**遗留**

- **线上还没更新。** 改动只在本地 `main`,`gh-pages` 未同步 —— 真机走查前需按 [DEPLOY.md](DEPLOY.md) 推一次,否则手机上测的还是旧逻辑。
- `ROLL_MIN=12` 是拍的数,依据是"12 道点起来不会觉得重复"。没做用户验证,日后觉得重复可以调大。
- 一周菜单 / 一日配餐的阈值(6 / 3)是各自独立拍的,没统一。目前没表现出问题,不动。

---

## 2026-08-24

### #016 · P8 部署准备:图标 + 部署分支 + DEPLOY.md(会话 #7)

**做了什么**

1. **生成 `apple-touch-icon.png`(180×180)。** 手写 SVG(暖米底 + 棕碗 + 三缕热气 + 1px 内框,贴合品牌「无阴影 / 1px 实线」)→ headless Chrome 3× 渲染成 540×540 → Pillow Lanczos 缩到 180×180。全程离线、零新增依赖。
   ```bash
   # 渲染(scratchpad/icon.html 是那份 SVG 包装)
   chrome --headless --disable-gpu --force-device-scale-factor=3 \
     --window-size=180,180 --default-background-color=00000000 \
     --screenshot=icon_3x.png icon.html
   # 缩放
   python -c "from PIL import Image; Image.open('icon_3x.png').convert('RGB').resize((180,180), Image.LANCZOS).save('apple-touch-icon.png','PNG',optimize=True)"
   ```

2. **`<head>` 加两行 `<link>`:** `apple-touch-icon`(加到主屏图标)+ `icon`(favicon,复用同一 PNG)。**关键坑:iOS 不认 `data:` URI 形式的 apple-touch-icon**,必须是真实文件,所以图标不能内联,得跟 `index.html` 同目录部署。

3. **建了本地 `gh-pages` orphan 分支(方案 C)**,只含 `index.html` + `apple-touch-icon.png`,内部文档不外露。更新流程写死在 [DEPLOY.md](DEPLOY.md)。

4. **写了 [DEPLOY.md](DEPLOY.md):** Gitee Pages 主线清单(注册→实名→建公开仓库→推 `main`+`gh-pages`→开 Pages→手机验收)+ 腾讯云 COS 兜底清单 + 日常更新流程 + FAQ。

5. **回填文档:** 新增 [ADR-022](DECISIONS.md);订正 ADR-021 里「apple-touch-icon 缺失归 P6」为已解决;更新 README(部署指引 + 文件清单 + 决策数 21→22)。

**实测(在本地 http server 上模拟托管,等效 https 行为)**

| 验收项 | 我能测的部分 | 结果 |
|---|---|---|
| #1 能访问 | `index.html` 200 | ✅ |
| #3 数据能存住 | 空 → 命中测试确认可点 → 派发真实事件选中西红柿 → 刷新 → 恰好留存这一项 | ✅ |
| #4 无存储警告 | http 下 `STORAGE_OK=true`,页面无「不让保存数据」红条 | ✅ |
| #5 图标 | `apple-touch-icon.png` 200、`<link>` 解析正确 | ✅(真机加主屏效果待用户实测) |
| #7 不回归 | `node check.js` → **0 error**,3 条营养 warning(±18–19%,设计带内),退出码 0 | ✅ |

**为什么用真实事件序列而不是 `element.click()`**
守红线 #4:先 `document.elementFromPoint(70,327)` 确认食材格中心真的点得到(`hitIsInsideIng=true`),再在该坐标派发 `pointerdown→mousedown→pointerup→mouseup→click`,走 App 真实的事件委托链路。`element.click()` 会绕过命中测试,不能用来证明"点得到"。

**补记(同会话,晚些):推送到 Gitee**
用户建好了公开仓库 `wangchen1995/eat-what`(注意带连字符,与 DEPLOY.md 原示例 `eatwhat` 不同,已订正文档)。加 remote 后 `git push -u origin main` + `git push -u origin gh-pages`,凭据走本机 Git Credential Manager(已缓存,未弹窗,我全程没经手密码)。`git ls-remote` 核验:远端 `main`=`a33b12a`、`gh-pages`=`a4bfae9`,与本地一致。方案 C 的红利兑现:本次只改了文档,`gh-pages` 那两个文件没动,**无需重新同步部署分支**。

**补记(同会话,再晚些):Gitee Pages 下线,改道 EdgeOne Pages**
推送后去开 Pages,发现仓库「服务」菜单 14 项服务里**没有 Gitee Pages**。查证([知乎](https://www.zhihu.com/question/655233802) / [Gitee 帮助中心](https://help.gitee.com/services/gitee-pages/pro/)):**免费 Gitee Pages 2024-05 整体下线,Pages Pro 停个人购买** —— 政策问题,非账号问题。正好命中 ADR-022 原文预留的"政策再变"兜底触发条件。

改走**腾讯云 EdgeOne Pages**(用户在选项里拍板):免费 + 国内 CDN + **支持直连 Gitee 仓库**(`push` 到 `gh-pages` 自动重部署),也支持直接拖文件上传。查实关键点:国内控制台 `edgeone.cloud.tencent.com`;纯静态站构建配置=构建命令留空 / 输出目录 `./` / 生产分支 `gh-pages`;**默认预览域名可直接访问、不需备案**,只有自定义域名在中国大陆加速区才要备案。COS 继续留作兜底。据此重写了 DEPLOY.md,并给 ADR-022 加了修订段。方案 C 不变、反而更贴合(输出目录 `./` + 分支 `gh-pages` 只上线两个文件)。

**补记(同会话,又晚些):EdgeOne 认不到 Gitee 分支,加推 GitHub**
EdgeOne Makers 控制台入口更正为 `console.cloud.tencent.com/edgeone`(用户先打开成了文档站);产品已改名 Makers,功能不变。在「导入 Git 仓库」连 Gitee 后,**生产分支下拉搜 `gh-pages` 显示「暂无数据」** —— EdgeOne 对 Gitee 分支枚举不行。遂把 `main` + `gh-pages` **加推一份到 GitHub `WangDuoYuOnebillion/EatWhat`**(本机没装 `gh`,仓库由用户网页建,我加 `github` remote 后推;凭据走 GCM,已缓存未弹窗)。`git ls-remote github` 核验:main `a7f7a7a`、gh-pages `a4bfae9`,与本地一致。EdgeOne 改连 GitHub,Gitee 降为镜像。DEPLOY / ADR-022 / STATE 均已回填。

**补记(同会话,收尾):EdgeOne 默认域名只有 3 小时 → 正式托管改用 Cloudflare Pages,已上线**
用户注册腾讯云、EdgeOne Makers 连 GitHub `gh-pages` **构建成功**,但成功弹窗写明:**默认域名仅 3 小时限时预览、去掉 `?eo_token=` 打不开,长期用必须绑自定义域名**(大陆加速区要备案)。即 EdgeOne 免费默认域名不能长期分享 —— 推翻了我此前"默认域名免备案可直接用"的判断(那条只对已下线的 Gitee Pages 共享备案域名成立)。
用户在选项里拍板改用 **Cloudflare Pages**(免费/永久/无 token/免备案)。我用 **Claude for Chrome** 在用户浏览器里操作:Workers & Pages → Create → Pages → Connect to Git → 授权 GitHub(sudo 二次验证的**密码由用户自己输,我不经手**)→ 选 `WangDuoYuOnebillion/EatWhat` → 生产分支 `gh-pages`、Framework=None、构建命令空、输出目录 `/` → Save and Deploy。构建日志确认拉的是 `gh-pages`(commit `a4bfae9`)。
**已上线:https://eatwhat-cui.pages.dev** —— 桌面浏览器打开验证:首页/网格/底栏正常,**顶部无存储红条**(https 下 localStorage 正常)。Cloudflare 盯 GitHub `gh-pages`,以后 `git push github gh-pages` 自动重部署。
**代价/风险:** Cloudflare 是海外服务器,国内微信能否稳定打开**只有真机实测算数**;不通则回退"买域名+备案+EdgeOne"(EdgeOne 项目已建好)。见 [ADR-022 终局修订](DECISIONS.md)。

**验收结果(用户真机实测,同会话收尾):**
- ✅ **手机浏览器能正常打开** `eatwhat-cui.pages.dev` 并使用。
- ❌ **微信内直接打不开** —— 海外未备案域名在微信里被拦(典型行为)。过渡办法:微信里点右上角「···」→「在浏览器打开」通常可用。要在微信内直接渲染,只能靠**备案的自定义域名**(留到日后,见备选一)。
- 用户决定:**接受现状,P8 到此收尾**。核心目标"有个能长期分享的免费网址"已达成(浏览器可开、永久免费免备案);"微信内一键直开"作为已知限制留待日后备案解决。

**P8 结论:✅ 完成(带一条已知限制)。** 应用已上线 https://eatwhat-cui.pages.dev,浏览器可用;微信内直开需日后备案域名。下一阶段按执行顺序为 **P2 手机真机走查**,新开会话。

**遗留 / 日后可选**
- 想让微信内一键直开 → 买域名 + ICP 备案 + 回连 EdgeOne(项目已建好),约 1–2 周。见 [DEPLOY.md](DEPLOY.md) 备选一。
- Cloudflare 上 `main` 分支每次 push 会生成一个 Preview 部署(噪音,不影响生产域名);嫌乱可在 Cloudflare 设置里关掉非生产分支构建。

---

## 2026-08-22

### #015 · 重构 ROADMAP:加执行顺序表、部署提前、新增多端阶段

**背景**
用户提出:上下文太长了,后面要分多个会话继续;需要一份能支撑分会话协作的开发步骤文档;下一步想先部署到免费托管(Gitee Pages);再往后可能做小程序或 H5。

**改了什么**

1. **加了「执行顺序」表,并把它定为"下一步做什么"的唯一权威。**
   阶段编号**只增不改、永不重排** —— WORKLOG / DECISIONS / STATE 里到处引用 P1、P4,重排编号会让这些引用集体失效。所以顺序单独用一张表表达,编号只是身份标识。这一点同时写进了 CLAUDE.md,因为它是新会话最容易踩的坑。

2. **新增 P8「部署到免费托管」并插到 P2 之前。** 三个理由:
   - 这是当前最卡的真实需求(用户要把应用发给别人,而只发 `index.html` 在 iPhone 上基本走不通)
   - **不先部署,P2 会白测一半** —— `file://` 下 iOS Safari 很可能禁 localStorage,而托管到 https 后这个问题直接消失。先测一个马上要作废的环境是浪费
   - P6「加到主屏」从 `file://` 基本走不通,托管后才谈得上

3. **P2 内容重写。** 因为 P8 提前,原来那批 `file://` 风险项作废,换成真正的移动端能力问题(静音键下计时音、输入法、安全区、长按菜单、中文输入法复核…),并补上已知设备 iPhone 13。

4. **新增 P9「多端:微信小程序」** —— 并澄清了一件容易误解的事:**「H5」在 P8 完成后就已经有了**。国内语境下 H5 基本就等于"能用链接打开的手机网页",托管完成的那一刻它就存在,不需要单独开阶段。真正的新工作量只有小程序。

5. **补记 P5.1**(改名 + iOS 适配 + 修中文搜索)—— 会话 #6 实际做了但路线图里没有这一格。

**关于小程序移植的判断(写进 P9,供以后决策)**

小程序不能跑 DOM,视图层必须重写。但这个项目逻辑与视图分得很干净:

| 部分 | 能否移植 |
|---|---|
| `ING`(122 项)/ `RECIPES`(153 道) | ✅ 原样搬,纯数据 |
| `match()` 打分算法、份数换算、清单合并 | ✅ 原样搬,纯函数零 DOM 依赖 |
| 全部 CSS、所有 `view*()` 渲染、事件委托 | ❌ 全部重写 |

粗估等于重做 40%。两条路里 `web-view` 套 H5 需要**已 ICP 备案的域名**,个人主体基本走不通,所以大概率只能原生重写。

**已标记的一处红线冲突:** 小程序要复用数据+算法,就得把它们抽成独立模块,这与红线 1「单文件零依赖」([ADR-001](DECISIONS.md))直接冲突。已写进 P9:开工前必须新增 ADR 决定取舍(维持单文件手工同步 vs 拆分 + 构建步骤内联回去),不许绕过。

**顺带解决的待决问题**
原「PWA 走哪条路」的两难(PWA 要 https,而我们是 `file://`)被 P8 吸收 —— 一旦托管,这个矛盾不存在了。已在 STATE 标记结案。

**影响文件:** `ROADMAP.md`(大改)、`STATE.md`(重写)、`CLAUDE.md`、`WORKLOG.md`

**遗留问题**
- P8 有三个必须用户先答的问题(仓库公开性 / 默认域名够不够 / Gitee 的备选),已列进 ROADMAP 和 STATE
- Gitee Pages 免费版需实名认证、且历史上政策调整过 —— 这一条我没有实时核实过,开工时要先确认现行政策

---

### #014 · git init + pre-commit 钩子

**做了什么**
把项目变成 git 仓库(`main` 分支),初始提交含全部 12 个文件。这解决了 STATE 待决问题 #4 —— 之前连续三个会话在同一批文件上叠加改动,没有任何 diff 可回溯。

**配置上的三个有意选择**

1. **身份设在仓库内,不动全局。** 全局 `user.name` / `user.email` 都没配。我只设了 repo-local 的(`wangchen75` / `wangchen75@hetao101.com`),不碰用户的全局配置。名字是从邮箱前缀取的,随时可改:`git config user.name "你的名字"`。

2. **`core.autocrlf=false` + `.gitattributes` 钉死 LF。** 这个项目从头到尾是 LF(每次构建都验过 CR=0),而 Windows 上 git 默认 `autocrlf=true` 会在检出时转成 CRLF —— 那会让 `index.html` 行尾变得不一致,而且 diff 会整片飘红。用 `* text=auto eol=lf` 显式钉死,比只依赖本地配置更可靠(配置不随仓库走,`.gitattributes` 走)。

3. **`.gitignore` 保持极简。** 项目零依赖零构建,没有产物目录要忽略,列的都是环境噪音(OS / 编辑器 / node_modules / 临时文件)。

**pre-commit 钩子**

放在 `.githooks/pre-commit`,提交前自动跑 `check.js`,不过就中止提交。

**关键设计:校验暂存区快照,不是工作区。** 用 `git show :index.html` 取出真正要提交的那一份来校验。否则会出现"工作区已经改好、暂存的还是坏的"却照样提交成功 —— 这是钩子最常见的失效方式。

其他细节:
- 只在 `index.html` 进入本次提交时才跑,不拖慢纯文档提交
- 没装 node 时只提醒不拦截(不能因为环境缺件就让人提交不了)
- `.githooks` 而不是 `.git/hooks`,因为后者不进版本控制。代价是 clone 后要跑一次 `git config core.hooksPath .githooks`,已写进 README

**验证**

| 测试 | 结果 |
|---|---|
| 初始提交时钩子自动触发 | ✅ 校验输出出现在提交之前,0 error 后正常提交 |
| 把主料 id 改错 → 提交 | ✅ 被拦住,HEAD 未变 |
| **暂存区是坏的、工作区已修好 → 提交** | ✅ **仍被拦住** —— 证实校验的是暂存快照而非工作区 |
| 测试后恢复 | ✅ `git diff HEAD` 无差异,`check.js` 退出码 0 |

**踩到的一个坑**

`chmod +x .githooks/pre-commit` 在 Windows 上不生效 —— `core.filemode` 默认 false,可执行位不会记进 index,提交后模式是 `100644`。在 Linux / macOS 上 clone 出来钩子直接跑不起来。用 `git update-index --chmod=+x` 显式设置,改成 `100755`。

**提交历史**

```
ecc8789 chore: 给 pre-commit 钩子加可执行位
76c404b init: 小付，今天吃什么？v1.0
```

**影响文件:** `.gitattributes`、`.gitignore`、`.githooks/pre-commit`(均为新增)、`README.md`、`STATE.md`、`WORKLOG.md`

**遗留问题**
- **没有远端,没有 CI。** 钩子只在本地生效,而且能用 `--no-verify` 绕过。真要强制约束得先决定推到哪(GitHub / Gitee / 自建)
- 历史是从当前状态起步的单个初始提交。之前 P0–P4 的演进过程只在 WORKLOG 里,git 里没有 —— 补造历史是伪造,不做

---

### #013 · P4 校验脚本落盘

**做了什么**
把只存在于临时目录的 `check.js` 写成仓库里的正式文件(约 260 行,零依赖,Node 运行)。它从 `index.html` 正则抽出数据层求值,然后跑 7 类校验。

**为什么必须有它**
菜谱和食材是纯手写数据,**退化是静默的** —— 改错一个 id、删掉一个食材,页面照样打开,只是某些食材永远搜不到菜。而项目是零依赖零构建(ADR-001 / ADR-011),没有编译期检查。这是唯一的质量护栏。

尤其是 PRD §10 的两条硬约束(每个常用食材 ≥4 道主料、零孤儿食材)—— 它们决定产品的核心承诺,肉眼完全守不住。

**7 类校验**
引用完整性(食材/厨具 id)、id 唯一性、用量与单位白名单、营养自洽(标称热量 vs PFC 折算,偏差 >20% 报错、>15% 提醒)、步骤完整(≥3 步 / 每步有耗时 / 有关键经验)、覆盖度、零孤儿。

**当前数据实测结果**

```
菜谱 153 道 · 食材 122 项 · 厨具 8 种 · 调味基础包 21 样
炒菜 52 / 炖焖 22 / 汤羹 19 / 凉菜 19 / 蒸菜 10 / 主食 17 / 早餐 9 / 烤·炸锅 5
步骤总数 848(平均 5.5 步/道)· 每份热量 47–610 kcal

错误 0
提醒 3   [营养] 拍黄瓜 18% / 凉拌豆芽 18% / 蒜泥蒸茄子 19%
PRD §10  常用食材主料 ≥4 道 32/32 ✓   零孤儿食材 122/122 ✓
退出码 0
```

那 3 条营养提醒都在 20% 阈值内,是估算固有的松动(凉菜的油量最难标准化)。**没有为了让报告好看去改数字** —— 校验的价值恰恰在于它会说实话。

**验收标准(P4 要求"能故意造 6 类错误各一个并被准确报出")**

做了 `--selftest`:克隆数据、逐类注入错误、断言对应类别的 error 被报出。实际做了 **8 类**(比要求多 2 类),**9/9 通过**(含"未注入时必须零 error"的基线)。

另外做了一次**端到端**验证(自检是内存态变异,不覆盖文件解析链路):真的把 `index.html` 复制一份改坏(主料 id 打错 + 删掉「牛奶」这个食材),`node check.js broken.html` 报出 8 条 error、退出码 1;原文件仍然零 error、退出码 0。

**用法**

```bash
node check.js              # 校验 ./index.html
node check.js --selftest   # 验证脚本本身没坏
node check.js other.html   # 校验指定文件
```

**影响文件:** `check.js`(新增)、`README.md`(新增「改完数据一定要自检」一节)、`ROADMAP.md`、`STATE.md`、`WORKLOG.md`

**遗留问题**
- 脚本没接到任何自动触发点(无 git hook / CI),要靠人记得跑。项目当前不是 git 仓库(STATE 待决 #4),`git init` 之后可以挂 pre-commit
- 营养自洽只能查"内部一致",查不出"标称值整体偏高/偏低"。要真正校准需要逐道对食材成分表核算,不在本阶段范围

---

### #012 · 改名 + iOS 适配 + 修中文搜索 + P1 纯手机化改造

**用户三条要求:**(1) 改名为「小付，今天吃什么？」(2) 适配 iPhone 手机 UI (3) 搜食材栏搜不了中文

**⚠️ 会话冲突说明(重要)**
本次会话与会话 #5(规划会话)**并行进行**。#5 期间我正在改 `index.html`,所以 #5 的 STATE.md 里那条「index.html 被外部改动过,不是我改的」指的就是本会话。已确认:`index.html` 与本会话的分块构建产物**逐字节一致**,没有第三方改动,#5 只动了文档。

发现 #5 建立的治理文档后,本会话补做了 P1(见下),因为用户「适配手机 UI」的要求正是 P1 的定义,且 ADR-019 已经把方向定了。**但流程上没有做到「开头复述阶段并确认」** —— 那些文档是在我开工之后才出现的。下个会话请照 CLAUDE.md 的流程走。

---

**① 修中文搜索(真 bug)**

根因不在匹配逻辑,而在渲染:`input` 事件里重绘整个 `#main`,把 `<input>` 元素销毁重建。中文拼音输入法靠 composition 组字会话工作,元素一没,组字会话跟着没,永远走不到选词。英文/拼音「看着能用」是因为不走组字。

修法:选食材页拆成静态区(搜索框)+ `#chipWrap` + `#pickBody`,输入时只重绘 `#pickBody`;加 `ev.isComposing` 跳过组字中过滤;补 `compositionend`。详见 [ADR-020](DECISIONS.md)。

顺带修掉一个手机体验问题:原来搜一样点一样会整页重绘导致**键盘收起**,现在焦点全程保持。顺手让搜索支持拼音(食材 `id` 本就是拼音,零成本)。

**② iOS / iPhone 适配** —— 13 项,逐条理由见 [ADR-021](DECISIONS.md)。最要紧的三条:
- 输入框统一 `16px`(iOS 聚焦小字号输入框会强制放大整页)
- `100dvh` 替 `100vh`(iOS 的 `100vh` 含地址栏,底部被裁)
- 计时提示音改为在「开始计时」的点击手势里 `ensureAudio()`(iOS 要求 AudioContext 必须在用户手势中创建/恢复,原来在计时结束那刻才 new,在 iOS 上必然静默)

**③ 改名** —— 标题、页头品牌、控制台前缀、README。品牌名变长,加了 `max-width:400px/344px` 两档字号收缩,实测 320–430px 全区间不裁切、不与🎲按钮重叠。

**④ P1 纯手机化改造**
- 删掉三处桌面断点(`min-width:640px` × 1、`min-width:1024px` × 2)
- 引入 480px **手机画框**。刻意**不用媒体查询**实现:`body` 用比 `--bg` 深一档的底色,`#app` 用本色 + `max-width:480px;margin:0 auto` —— 手机上 `#app` 撑满,深色底永远看不见;桌面上自然露出画框两侧。边界用 `outline` 不用 `border`,因为 outline 不占布局宽度,手机上被画到屏幕外,不吃那 2px
- 四个 `position:fixed` 元素收进画框(`.tabbar` / `.stickycta` / `.sheet` / `.cook`),统一用 `left:50%;transform:translateX(-50%);width:min(var(--frame),100%)`。`.scrim` 遮罩例外,保持全屏
- 删掉 `@media(min-width:700px)`(做菜模式大字号)—— 这解决了 STATE.md 待决问题 #3。理由比原倾向更充分:加了 480px 画框后,视口 ≥700px 只意味着「桌面」,而做菜层被画框限成 480px 宽,31px 字号在这个宽度里会撑爆。25px 在 320–480px 全区间都成立
- 清掉 `main{max-width:1120px}` 和 `.stickycta .inner{max-width:1120px}` 两处桌面遗留

**⑤ 实测发现并修掉的一个真 bug**

命中测试报告选食材页有 5–6 个食材格被 `.stickycta` 拦截。查下来是真的:`main` 底部留白只有 ~84px,而底部 Tab 栏(58px)+ 吸底 CTA(~62px)合计遮住约 120px —— **最后一排食材格被永久盖住,滚动也露不出来**。吸底 CTA 是 `fixed` 的不占文档流,必须在流里补一块等高占位。已加 `.ctaspacer{height:70px}`。

这个 bug 从截图上看不出来(看起来只是列表到底了),只有命中测试能抓到。

**P1 六条验收标准实测**

| # | 标准 | 结果 |
|---|---|---|
| 1 | 320/360/375/390/414/480px 六宽度 × 8 视图零横向溢出 | ✅ 全部 `docScrollW == docClientW`,页面级溢出元素 0 |
| 2 | `elementFromPoint()` 逐控件命中,可达率 100% | ✅ 零真实拦截(被 Tab 栏/CTA 遮挡的已验证「底部留白足够滚出」) |
| 3 | 覆盖物审计零违规 | ✅ 8 视图 × 7 宽度全部 0 |
| 4 | 桌面 1280px 画框对齐误差 ≤1px | ✅ `#app`=480px;`.tabbar`/`.stickycta`/`.sheet` Δleft=Δright=0.0;`.scrim` 保持全屏;做菜层 480px |
| 5 | `grep min-width:1024px\|min-width:640px` = 0 | ✅ 0 |
| 6 | 功能不回归 | ✅ 37 项功能断言 + 22 项中文搜索断言全部通过 |

**测试方法上的两次自我纠错**(又踩了同一类坑,方法论教训见 STATE.md 结尾)
- 命中测试报 `detail` 视图遮罩违规 —— 是过渡中间态(`.on` 已加但 opacity 还在动画中)。注入 `transition:none` 后消失
- 报 `.scrim` 宽度 1265 ≠ 1280 —— 差的是滚动条宽度,该用 `clientWidth` 而非 `innerWidth` 比对;报做菜层宽度 0 —— 未激活时 `display:none`。两条都是断言口径写错

**影响文件:** `index.html`、`PRD.md`、`DECISIONS.md`(+ADR-020/021)、`ROADMAP.md`、`STATE.md`、`README.md`、`WORKLOG.md`

**遗留问题**
- 真机仍未验(P2)。用户正在 iPhone 上用,是验证 P2 的好时机
- `apple-touch-icon` 仍缺,加到主屏没图标(P6)
- `check.js` 校验脚本仍不在仓库里(P4)

---

### #011 · 方向收窄为只做手机端,并把后续工作拆成阶段化交付

**背景(用户提出三条新要求)**
1. 不要在一个会话里全部做完 —— 设计多个开发步骤,分不同会话推进
2. 要有文档能让**新会话打开时立刻知道当前的开发节奏和重点**
3. **只做手机端,不是 PC 端**

**做了什么**

新增三份文档,填的是原来文档三件套没覆盖的空白 —— 原来有"要做什么"(PRD)、"为什么这么选"(DECISIONS)、"做过什么"(WORKLOG),缺的是**"现在该做什么"**:

| 文档 | 职责 | 解决的问题 |
|---|---|---|
| [CLAUDE.md](CLAUDE.md) | 项目总纲、8 条红线、会话开头/结尾流程、实现坐标速查 | 新会话第一眼该看什么 |
| [ROADMAP.md](ROADMAP.md) | P0–P7 七个阶段,每阶段含任务清单 + 可实测的验收标准 | 阶段怎么切、切到哪算完 |
| [STATE.md](STATE.md) | 当前在哪、下个会话第一件事、待决问题、已知限制 | 跨会话不丢上下文 |

**为什么把"下一步"单独拆一份文档**
WORKLOG 是**过去时**的,追加式、倒序、越写越长;要从里面反推"现在该干什么"得读一大段。STATE.md 是**现在时**的,每次覆写、永远只有一页,并且明确写了"下个会话开工的第一件事"的可照抄步骤。两者职责不重叠,不能合并。

**方向收窄:只做手机端**

原方案是"移动优先 + 桌面自适应",维护三档布局。现改为只维护一套移动布局,桌面浏览器下收进 480px 居中画框。记为 [ADR-019](DECISIONS.md)。

支撑这个决定的一条硬证据:**WORKLOG #010 那次整页点击失效,第二处成因就是桌面断点带来的** —— `≥1024px` 下 `.sheet` 居中定位 + `opacity:0` 会挡住屏幕中央,而移动端因为抽屉被 `translateY(101%)` 推到屏幕外反而没暴露。也就是说,**桌面布局制造了一个真实事故,而它没有任何用户。**

**改动的文件**
- 新增 `CLAUDE.md` / `ROADMAP.md` / `STATE.md`
- `DECISIONS.md`:新增 ADR-019;「待定问题」里的 PWA 一条升级为 ROADMAP P6 的开工前必答题
- `PRD.md`:§8「响应式」改写为「目标平台与布局:只做手机端」(含允许/禁止的媒体查询清单、画框对齐要求);§9 新增「目标平台」行、「兼容」行改为以真机为必测主目标;§11 布局验收表标注为 v1.0 旧数据并给出 P1 后的新口径(6 个手机宽度 + 桌面画框对齐);文档头部加跨文档导航
- `README.md`:项目文件表补三份新文档

**本次会话没做任何代码改动。** 按用户"一个阶段一个会话"的要求,纯手机化改造本身是 P1,留给下一个会话。这一点值得记:**规划本身就是一个阶段的产出**,把它和实现挤在同一个会话里,就等于又违反了刚定下的约定。

**发现的一个问题(已记入 STATE.md 已知限制)**
WORKLOG #005 提到写过 `check.js` 做数据完整性和覆盖度审计,但**它不在仓库里** —— 当时是一次性用完就丢的。这意味着 PRD §10 的两条硬约束(32 个常用食材各 ≥4 道主料、零"勾了没菜"的食材)目前**没有任何机制守着**,加减菜谱会悄悄退化。已排成 ROADMAP P4,并列为 P5 扩库的前置依赖。

**遗留问题**
4 个待决问题需用户回答,已列在 [STATE.md](STATE.md):PWA 路线选择、真机测试机型、`min-width:700px` 是否保留、是否 `git init`。其中只有 PWA 那条会真正卡住阶段(卡 P6)。

---

### #010 · 修复整页点击失效(用户反馈)

**症状**
用户反馈"页面点击没反应"。所有按钮全部失效,整个应用不可用。

**根因**
抽屉遮罩 `#scrim` 是 `position:fixed; inset:0; z-index:60`,我只用 `opacity` 切显隐,没有切 `pointer-events`。`opacity:0` 的元素看不见但**依然参与命中测试** —— 一层不可见的全屏遮罩常驻页面顶层,把每一次点击都吃掉了。

同一个错误还有第二处:桌面断点(≥1024px)下 `.sheet` 是居中定位 + `opacity:0`,会挡住屏幕中央。移动端因为它被 `translateY(101%)` 推出屏幕才没暴露。

**修复**
- `.scrim` 加 `pointer-events:none`,`.scrim.on` 加 `pointer-events:auto`
- `.sheet` 加 `pointer-events:none`,`.sheet.on` 加 `pointer-events:auto`(同时覆盖桌面断点)
- 做菜模式 `.cook` 用的是 `display:none`,本来就不受影响,无需改动

**为什么之前的测试没抓到 —— 这条最值得记**

之前 60 多项验收断言全部用 `element.click()` 程序化触发。**这种调用直接在元素上派发事件,完全绕过浏览器的命中测试。** 所以:

- 断言全部通过,因为它们测的是"事件处理逻辑对不对"
- 而实际上一个按钮都点不到,因为没有任何一项测的是"这个元素点得到吗"

`.click()` 能验证**可达之后会发生什么**,验证不了**是否可达**。这是我这次最严重的方法论漏洞 —— 高覆盖率的测试给了虚假的信心。

**补上的测试手段**(已写入 PRD §11 作为强制验收项)
1. `document.elementFromPoint(控件中心)` 逐控件命中测试:最顶层元素必须是该控件本身或其后代
2. 全屏覆盖物审计:扫出所有 `fixed`/`absolute`、覆盖视口 ≥25%、`opacity < 0.05` 却仍 `pointer-events:auto` 的元素

**验证结果**

| | 修复前 | 修复后 |
|---|---|---|
| 不可见拦截物 | `DIV#scrim` 覆盖 **100%** 视口 | **0 个** |
| 控件可达率 | **0 / 48**(全部被 `#scrim` 拦截) | **100%**(6 个非模态视图) |
| 抽屉自身控件 | — | 全部可点(3 底部按钮 + 关闭 + 5 份数按钮) |
| 做菜模式自身控件 | — | 全部可点(上一步/下一步/关闭) |
| 桌面 1280 / 1440px | `.sheet` 挡住中央 | `pointer-events=none`,全部可点 |

**过程中的一个岔路**
第一轮命中测试里,抽屉内部控件报"视口外",一度以为抽屉没滑上来。查下来是 headless Chrome 的 `--virtual-time-budget` 下 CSS 过渡不推进,测到的是过渡起始态。注入 `*{transition:none !important}` 排除时序变量后,抽屉 `rectTop=88`,一切正常。**测量环境的行为要先证伪,再信它的结论** —— 这是第二次踩同类坑(第一次见 #006 的视口下限)。

**遗留问题**
无。已排查全部浮层:`#scrim`、`.sheet`、`.cook`、`#toast`。其中 `.cook` 用 `display:none`、`#toast` 本来就有 `pointer-events:none`,均无此问题。

---

### #009 · 文档回填至与实现一致

**做了什么**
- [PRD.md](PRD.md) §6.1 调味基础包补全为实际的 21 样;§6.6 新增「零输入保护」条款并写清根因;§9 无障碍改为三级点击目标标准;§10 内容规格全部换成实测数字(153 道 / 848 步 / 122 项食材 / 厨具覆盖 148 of 153 / 热量 47–610 kcal);§11 验收清单换成 21 项功能 + 7 视图布局 + 5 项视觉规范的实测结果表;性能指标的道数订正
- [DECISIONS.md](DECISIONS.md) 新增 ADR-012 ~ ADR-017;并回头订正 ADR-002 / 004 / 007 / 011 里已经过时的「120 道」和旧基础包清单,补上校验脚本作为「无构建期检查」的对冲手段

**为什么**
文档和实现对不上就等于没有文档。这一轮改动里有 5 处是实现变了(基础包 21 样、零输入短路、点击目标、菜谱 153 道、替代方案文案),如果只改代码不回填,下次看 PRD 会被误导。

**遗留问题**
无。

---

### #008 · 视觉与一致性打磨

**做了什么**
- 菜卡热量从标签行里挪出来独立成行(`.kcalrow`)。原先热量和标签同处一个 `flex-wrap` 容器,带辣度标签的卡片会把热量折到第二行,同排卡片高度参差
- 替代方案前缀 `↳`(U+21B3)换成文字「替代 · 」—— 实测在字体回退下渲染成近似 "l," 的乱码。记为 [ADR-017](DECISIONS.md)
- 点击目标放大:分段控件 38→44px、图标按钮 38→44px、小按钮 34→38px;库存与配餐行内的图标按钮单独收到 40px,避免窄屏下两个 44px 按钮挤爆行。记为 [ADR-012](DECISIONS.md)

**为什么**
前两项是实测截图里看出来的,不看真实渲染发现不了。第三项是 PRD §9 的硬指标没达到 —— 与其降标准,不如把高频控件真的做够。

**遗留问题**
做菜模式在超高视口(>1200px)下步骤文字垂直居中会显得空旷。真机 812px 高度下正常,暂不处理。

---

### #007 · 浏览器实测,修掉 2 个真 bug

**做了什么**
在真实浏览器里跑了 60+ 项断言,驱动真实 DOM 与状态。修复:

1. **零勾选时返回 26 道「差一两样」**(真 bug)。根因:单主料 + 全基础包调味的菜在零勾选时 `missMain=1 / missTotal=1`,恰好落进 Tier 1 的条件。修法:`match()` 开头加零输入短路。记为 [ADR-013](DECISIONS.md)
2. **黑胡椒不在调味基础包里,而胡椒粉在**(真 bug,内部不一致)。导致黑椒牛柳、烤鸡翅等被「缺黑胡椒」压出 Tier 0。修法:纳入基础包(20→21 样)。记为 [ADR-014](DECISIONS.md)
3. **摆一摆候选耗尽后可能立刻重复刚摆出的那道**(体验缺陷)。修法:重置已摆记录时保留当前项。实测 20 次零连续重复
4. **localStorage 不可用时静默丢数据**。修法:启动时探测,不可用则在两个页面顶部显示明确提示。记为 [ADR-016](DECISIONS.md)

**三次测试自身写错的记录**(值得留下,避免以后重犯):
- 断言「勾上烤箱后烤箱菜出现」时没勾鸡翅/南瓜/芦笋,那 3 道菜是因为缺主料被排除的,与厨具无关
- 断言「青椒跨菜合并为 4 个」时,以为肉末茄子用青椒,实际用的是**尖椒**
- 断言「料齐时提示什么都不用买」时,忘了桂皮和香叶**不在**基础包里,清单本就非空

三次都是我的前提错了而不是代码错了。教训:断言前先核对配方数据,别凭印象。

**遗留问题**
无。

---

### #006 · 布局实测方法的两次返工

**做了什么**
- 第一次:用 headless Chrome `--window-size=390,1500` 截图,看到食材网格露出第 4 列、chip 行被切、底部 Tab 第 5 项被挤出屏幕,以为有严重溢出
- 排查发现 **headless Chrome 把视口下限钳在 500px**,`--window-size=390` 拿不到 390 的 CSS 视口;它按 526px 布局却只截了 390 逻辑宽,所以看起来像溢出。真实数据是 `docScrollW == docClientW`,零溢出
- 第二次:改用 **375px 宽的 iframe 画框**约束内嵌文档,配 `--allow-file-access-from-files` 让父页面读得到 iframe 内部。这样才拿到真实的 `innerWidth=375`
- 结果:7 个视图全部 **0px 横向溢出、0 个页面级溢出元素**。`catnav` 分类栏内有元素超右边界属预期(该栏是横向滚动容器),诊断脚本会自动识别祖先里的滚动容器并区别标注

**为什么**
差点因为测量工具的假象去"修"一个不存在的 bug。工具的行为要先验证,否则测出来的东西比不测更有害。

**遗留问题**
无。

---

### #005 · 覆盖度审计,菜谱库 124 → 153 道

**做了什么**
写了构建期校验脚本(`check.js`),除了数据完整性还审计覆盖度,发现两个真问题:
- **11 个食材完全没被任何菜用到**:娃娃菜、荷兰豆、红薯、秋葵、芦笋、培根、带鱼、鱿鱼、酸奶、平菇、黄豆 —— 用户勾了等于白勾
- **13 个「常用」食材的主料出现次数不足 4 道**,违反 PRD §10。其中胡萝卜只有 1 道菜拿它当主料(却在 23 道菜里当配料),玉米、西兰花、生菜同样只有 1 道

补了 29 道菜,每道针对性覆盖多个缺口。同时删掉唯一真正未被使用的调味「酵母」。

**为什么**
没有选择降低 PRD 的覆盖度标准去迁就实现。这条要求的真实目的是"勾任何常见食材都能出有意义的结果",降标准等于放弃这个保证。详见 [ADR-015](DECISIONS.md)。

**结果**
32 个常用食材全部 ≥4 道主料,零"勾了没菜"的食材,且这两条现在由校验脚本强制,以后加减菜谱不会悄悄退化。

**遗留问题**
无。

---

### #004 · 应用构建完成

**做了什么**
构建 `index.html`(单文件,3900 行 / 246KB),11 个分层:

| 层 | 内容 |
|---|---|
| 样式层 | 设计 token、纸纹、移动优先布局、三档断点、8 个分类渐变 |
| 数据层 1 | 8 种厨具、7 个食材分类、8 个菜品分类、122 项食材(含别名 / 默认保质天数 / 荤素标记)、21 样调味基础包、24 条通用替代方案 |
| 数据层 2 | 153 道菜谱结构化数据 |
| 状态层 | localStorage 持久化 + 存储可用性探测 |
| 工具层 | 份数换算(g 取整到 5,个/片/勺保留 0.5)、剩余天数、替代查询 |
| 匹配层 | 硬过滤 → 缺料计算 → 三档分级 → 加权打分 |
| 渲染层 | 5 个 Tab 视图 |
| 详情层 | 菜谱详情抽屉、忌口设置、库存录入、一周菜单 |
| 摆一摆层 | 随机、一日配餐、单格换菜 |
| 做菜模式 | 全屏单步、计时器(WebAudio 提示音 + 标题闪烁)、Wake Lock |
| 事件层 | 统一委托 + 键盘支持(Esc / ←→ / 空格) |
| 自检层 | 启动时校验数据引用完整性并输出到控制台 |

**构建方式**
分 6 块写入临时目录再拼接。原因:单文件近 4000 行,一次性写入不可靠;分块还能让菜谱数据独立于逻辑演进。

**过程中的两个坑**
- Windows 下 heredoc 因行尾符匹配不上终止符而失败,改用文件写入工具
- 用 Python 改写分块时把行尾转成了 CRLF,污染了交付文件的行尾一致性。已全部规范化回 LF 并在每次拼接后校验

**遗留问题**
无。

---

## 2026-08-21

### #003 · 建立文档基线

**做了什么**
- 写出 [PRD.md](PRD.md):13 章,含背景与问题定义、竞品结论、用户场景、7 个 Epic 的用户故事与验收标准、信息架构、匹配算法完整规格、数据模型、视觉规范、非功能需求、内容规格、DoD 验收清单、范围外说明
- 写出 [DECISIONS.md](DECISIONS.md):11 条 ADR,覆盖交付形态、推荐引擎、匹配算法、调味基础包、库存录入、视觉、配图、份数基准、厨具层级、热量诚实性、零依赖
- 建立本日志

**为什么**
用户要求"目标统一"。PRD 定义要做什么和验收标准,DECISIONS 记录为什么这么选(防止后续反复推翻),WORKLOG 记录做了什么。三者职责不重叠。

**遗留问题**
- PRD §10 的菜谱分类道数是目标值,实际内容制作时可能微调,完成后需回填实际值
- 验收清单(§11)在开发完成后需逐项实测勾选

---

### #002 · 需求澄清完成,11 项决策全部锁定

**做了什么**
两轮共 11 个问题问完,全部决策锁定:

| 维度 | 决定 |
|---|---|
| 交付形态 | 单文件 `index.html`,双击即用,离线,localStorage |
| 推荐引擎 | 内置离线菜谱库 + 规则打分匹配 |
| 内容量 | 120 道中式家常菜 |
| 视觉风格 | 暖米色杂志风(`#F5F0E8` / `#8B4513` / 衬线标题 / 1px 实线 / 无阴影) |
| 配图 | emoji + 按分类生成的渐变色块 |
| 设备优先 | 手机浏览器为主,桌面自适应 |
| 默认份数 | 3 人份基准,可切 1/2/3/4/6 |
| 厨具筛选 | 要,做成轻量设置项 |
| 功能范围 | 核心推荐 + 库存临期 + 忌口过滤 + 营养目标 + 缺料替代购物清单 + 做菜模式 + 摆一摆配餐 + 收藏历史 |
| 文档形式 | 项目内 Markdown 三件套 |

**过程中的一个澄清**
第一轮功能范围题用户选了"其他"但未留文字。正文里列了 8 个候选功能而选项框只能放 4 个,因此追问确认 —— 结果是剩下 4 个(做菜模式、摆一摆配餐、收藏历史、份数联动)也全部要做。同时补问了两个遗漏项:默认份数、厨具筛选层级。

**遗留问题**
无。

---

### #001 · 竞品调研

**做了什么**
调研 8 个同品类产品,分三类:
- **海外食材反查工具:** SuperCook、Pantry Pic、Frigo Magic
- **国内菜谱与 AI 生成:** 下厨房、隔离食用手册 / cook(开源)、今天吃什么 AI 智能食谱
- **食材保质期工具:** 食光冰箱、保质记
- **内容库参考:** HowToCook(程序员做饭指南,开源)

**提炼出 7 条设计原则**(已写入 PRD §2.2,直接构成设计约束):
1. 输入成本是生死线 → 分类网格勾选,绝不做纯输入框
2. 匹配不能是布尔运算 → 三档分级 + 打分,永不返回空
3. 缺料要正面展示 → 缺料清单 + 替代方案 + 一键进购物清单
4. 不要把首页让给非核心内容(下厨房的教训)→ 首页只做选食材 → 出菜
5. 做菜模式必须独立 → 全屏、大字、计时器、屏幕常亮
6. 热量一眼可见 + 可展开 → 概览条 + 展开营养详情,份数联动
7. 临期管理的通病是录入成本 → 只问"还能放几天"

**最关键的三个发现**
- **cook(隔离食用手册)的"模糊 / 严格 / 生存"三档匹配**是本品类最聪明的设计,直接借鉴为 Tier 0/1/2
- **食光冰箱类产品的死因是录入成本**(要填生产日期 + 保质期),这是我们的差异化机会
- **下厨房的教训是商业化侵占首页**,用户最终只用搜索功能

**遗留问题**
- HowToCook 的菜谱可作为步骤精确性的校对参考,但它是 Markdown 且无热量数据,需重新结构化 —— 本项目菜谱数据为独立整理,不直接复制其内容

---

## 待办队列

### 已完成
- [x] 构建 `index.html`:结构与样式层
- [x] 食材库数据(122 项,含分类 / emoji / 别名 / 默认保质天数 / 荤素标记)
- [x] 菜谱库数据(153 道,分批构建 + 覆盖度补充)
- [x] 匹配算法与打分实现(硬过滤 → 缺料 → 三档 → 加权)
- [x] 五个 Tab 页面的渲染逻辑
- [x] 菜谱详情 + 做菜模式(计时器 / Wake Lock)
- [x] 摆一摆 / 一日配餐 / 一周菜单
- [x] 购物清单合并逻辑(跨菜同食材同单位累加)
- [x] 构建期校验脚本(数据完整性 + 营养自洽 + 覆盖度)
- [x] 逐项跑 PRD §11 验收清单(21 项功能 + 7 视图布局 + 5 项视觉规范)
- [x] 回填 PRD §10 实际菜谱统计
- [x] 文档三件套与实现对齐
- [x] 真实命中测试(`elementFromPoint`)+ 全屏覆盖物审计 —— 因 #010 的整页点击失效补上,现为强制验收项

### 未做,已归入阶段

> 阶段划分与验收标准见 [ROADMAP.md](ROADMAP.md);下个会话该做什么见 [STATE.md](STATE.md)。

| 待办 | 归入阶段 |
|---|---|
| 砍掉桌面断点,改为单一移动布局 + 480px 桌面画框 | **P1**(下一个) |
| `file://` 下 localStorage 的真机验证(Safari 策略最严,已有 [ADR-016](DECISIONS.md) 兜底但未真机确认) | **P2** |
| iOS Safari / Android Chrome 真机走查:Wake Lock、WebAudio 提示音、`env(safe-area-inset-bottom)`、输入法遮挡、滚动惯性 | **P2** |
| 抽屉下滑关闭手势、`overscroll-behavior`、触觉反馈、长列表帧率 | **P3** |
| `check.js` 落盘为仓库文件(现在不在仓库里,覆盖度约束无人守) | **P4** |
| 菜谱库 153 → 200,高频菜步骤与热量二次校对 | **P5** |
| 添加到主屏 / PWA(**开工前需用户选定路线**) | **P6** |
| 做菜模式在超高视口(>1200px)下步骤文字居中显得空旷(优先级低) | P7 或不做 |

### 不做(范围外,见 PRD §12)
账号 / 云同步、拍照识别食材、真实菜品照片、菜谱社区、买菜电商跳转、视频教程、AI 动态生成菜谱
