# 决策记录 (Decision Log)

记录关键的产品与技术决策、当时的备选方案、以及为什么这么选。目的是让后续的改动有据可依,避免反复推翻。

格式:`ADR-序号 · 决策标题` → 背景 / 选择 / 备选 / 理由 / 代价。

---

## ADR-001 · 交付形态:单文件离线 Web App

**日期:** 2026-08-21 · **状态:** 已确认(用户确认)

**背景:** 需要决定用什么技术形态承载这个应用。

**选择:** 单个 `index.html`,内联全部 HTML / CSS / JS 与菜谱数据,localStorage 持久化。

**备选:**
- React + Vite 工程 —— 组件化、易扩展,但每次要 `npm run dev`,依赖本地 Node 环境
- 微信小程序 —— 手机体验最好,但需 AppID + 开发者工具,本环境无法预览调试
- 网页 + Python 后端 —— 可多设备同步,但对"一个人做家常菜"是过度设计

**理由:** 使用场景是"下班回家站在冰箱前打开",最重要的是**零启动摩擦**。双击文件就能用、断网能用、换台电脑复制一个文件就能用。这三点任何工程化方案都做不到。

**代价:**
- 单文件体积大(数千行),代码组织靠严格的分区注释来维持可读性
- 不能用 ES Module(`file://` 协议不支持),必须写成单个 IIFE / 全局作用域
- 无构建期检查,靠人工纪律保证质量

---

## ADR-002 · 推荐引擎:内置离线菜谱库 + 规则打分

**日期:** 2026-08-21 · **状态:** 已确认(用户确认)

**选择:** 手工整理结构化菜谱数据(初版目标 120 道,最终 153 道,见 ADR-015),前端做规则打分匹配。

**备选:**
- 调用 Claude API 动态生成 —— 菜谱无限,但必须联网、要 API Key 和费用、同样输入结果不稳定、热量是模型估的无法校对
- 混合方案(离线为主 + AI 补充)
- 第三方菜谱 API —— 国内可用性和中式菜谱覆盖都差

**理由:** 这个产品的核心承诺是"照着做能做出来"。菜谱内容的**可校对性**比数量更重要 —— 一道步骤错误的菜比少十道菜的伤害大得多。离线库的每一道菜的用量、火候、时间、热量都是可审查、可修正、可复现的。而 LLM 生成的菜谱同一次输入两次结果不同,出了问题无法定位和修复。

**代价:** 菜谱数量有硬上限,冷门食材组合可能只落到"凑一凑"档。内容制作是主要工作量。

**已记录的后续可能:** 保留将 AI 生成作为**可选增强**的空间(PRD §13),但绝不改变离线主路径。

---

## ADR-003 · 匹配算法:三档分级 + 加权打分,而非布尔过滤

**日期:** 2026-08-21 · **状态:** 已确认

**选择:** Tier 0 现在就能做 / Tier 1 差一两样 / Tier 2 凑一凑也行,档内按分数排序。

**备选:** 严格 AND 匹配(食材全有才推荐)。

**理由:** 竞品调研的最重要结论。严格匹配下,用户勾 5 样食材大概率返回 0 条结果,而**空结果是这个品类最大的体验杀手** —— 用户会立刻关掉再也不开。开源项目 cook(隔离食用手册)的"模糊 / 严格 / 生存"三档是同类产品里最聪明的设计,直接借鉴。

**关键设计细节:** Tier 1 的条件是 `missTotal ≤ 2 且 missMain ≤ 1` —— 双重约束。允许差 2 样辅料或调味,但**最多只允许差 1 样主料**。差 2 样主料的菜本质上已经是另一道菜,放进"差一两样"会摧毁用户对分档的信任。

---

## ADR-004 · 调味料默认视为"家里都有"(调味基础包)

**日期:** 2026-08-21 · **状态:** 已确认

**选择:** 引入"调味基础包"概念,默认开启,共 21 样(盐、食用油、糖、生抽、老抽、醋、料酒、葱、姜、蒜、淀粉、胡椒粉、蚝油、鸡精、香油、干辣椒、冰糖、八角、花椒、白芝麻、黑胡椒 —— 最后一项见 ADR-014)。这些不计入缺料,可在设置中关闭。

非基础包的 11 样调味(豆瓣酱、芝麻酱、番茄酱、咖喱粉、孜然、五香粉、辣椒面、桂皮、香叶、蜂蜜、可乐)**故意留在包外** —— 没有豆瓣酱确实做不了麻婆豆腐,这个缺口是真实信息而不是噪音。

**理由:** 如果调味料也要用户勾选,那么几乎每道菜都会显示"差 5 样",分档立刻失效,整个产品的信号全被噪音淹没。SuperCook 也是这么处理的(pantry staples)。而现实中家里有炒锅的人基本都有生抽和盐。

**代价:** 极少数真的没有蚝油/老抽的用户会看到"能做"但实际差料。缓解措施:菜谱详情页的调味段落**完整列出**所有调味料,并对不常见的给替代方案;设置里可一键关闭基础包改为严格模式。

---

## ADR-005 · 库存录入只问"还能放几天"

**日期:** 2026-08-21 · **状态:** 已确认

**选择:** 点食材后弹六个快捷按钮(1天 / 3天 / 1周 / 2周 / 1个月 / 长期),按食材类型智能预填默认值。不要日期选择器,不问生产日期,不问保质期。

**备选:** 像食光冰箱 / 保质记那样填生产日期 + 保质期,系统算到期日。

**理由:** 竞品最大的失败点就在这里。调研结论明确:"录入时需要输入的内容繁多,不仅要拍照,还要输入生产日期、保质期、到期时间" —— 用户填两次就放弃了。而对做饭决策来说,精确到日期毫无价值,**"快坏了 / 还能放 / 不着急"三档就够用**。用户脑子里本来就是这么想的("这菠菜再放两天就不行了")。

**代价:** 不精确。但精确性在这里是伪需求。

---

## ADR-006 · 视觉:暖米色杂志风,无阴影,系统衬线字体

**日期:** 2026-08-21 · **状态:** 已确认(用户在四个方案中选定)

**选择:** `#F5F0E8` 米色底 / `#8B4513` 赭石主色 / 衬线体标题 / 1px 实线分隔 / 无阴影 / 4px 小圆角。

**关键技术约束:** **不能用 Google Fonts 加载 Noto Serif SC** —— 违背离线要求。改用系统衬线字体栈:

```
'Noto Serif SC','Source Han Serif SC','Songti SC','STSong','SimSun',serif
```

Windows 命中宋体 / 思源宋体,macOS 命中宋体-简,Android 降级到系统衬线。三平台都能得到衬线观感,只是字形不完全一致。这是离线约束下的正确取舍 —— 宁可字形不统一,也不能为了字体去联网。

**理由:** 用户在四个风格方案中明确选择。杂质感和"纸质食谱书"的气质与做饭这件事的调性契合,也和市面上千篇一律的圆角卡片 + 阴影的 App 拉开距离。

**代价:** 信息密度略低于纯 sans-serif 方案;不同平台字形有差异。

---

## ADR-007 · 菜品配图:emoji + 按菜系生成的渐变色块

**日期:** 2026-08-21 · **状态:** 已确认(用户确认)

**选择:** 每道菜一个代表性 emoji,居中放在按菜品分类算法生成的双色柔和渐变块上。

**备选:**
- 手绘 SVG 插画 —— 更精致,但上百道菜的插画工作量巨大且质量不稳定
- 留占位后填真实照片 —— 第一版会是一堆占位框
- 完全不要图 —— 菜谱类应用没有视觉锚点,浏览时难以扫读

**理由:** 单文件离线是硬约束,真实照片意味着 base64 内嵌(体积爆炸)或外链(违背离线)。emoji 是**零体积、零版权风险、跨平台已有渲染**的视觉方案。渐变色按分类生成,保证同类菜色调一致,浏览时形成可扫读的视觉节奏。

**代价:** 不是真实菜品照片,无法"看图激发食欲"。用色彩体系和排版质感部分弥补。

---

## ADR-008 · 份数基准定为 3 人份

**日期:** 2026-08-21 · **状态:** 已确认(用户选择 3–4 人家庭)

**选择:** 所有菜谱的 `qty` 用量以 3 人份存储,UI 默认 3 人,可切 1 / 2 / 3 / 4 / 6。

**营养数据的换算约定(容易搞错,明确记录):**
- `nutr` 存的是**每份**数值,**不随份数变化**
- 食材用量 = `qty × 份数 / 3`
- 合计热量 = `每份 kcal × 份数`
- UI 同时显示"每份 XXX kcal"和"合计 XXX kcal",避免用户误读

**理由:** 用户明确说是一家人吃饭。以 3 人份为基准,向上到 4/6 人和向下到 1/2 人的换算误差都最小(若以 1 人份为基准,乘 6 会放大取整误差)。

---

## ADR-009 · 厨具做成轻量设置项,不进主流程

**日期:** 2026-08-21 · **状态:** 已确认(用户选择)

**选择:** 设置页勾选一次拥有的厨具,之后默默过滤。炒锅 + 汤锅 + 蒸锅默认已勾选。提供"忽略厨具限制"开关。

**备选:** 像 cook 那样做成主流程第二步(选食材 → 选厨具 → 出结果)。

**理由:** 厨具是**低频变化**的属性 —— 家里有没有烤箱一年也不会变一次。放进主流程等于每次做饭都要为一个不变的事实多点一次。设置一次永久生效才是正确的交互层级。

**代价:** 新用户如果不去设置页,烤箱菜会被默认隐藏(因为烤箱默认不勾)。缓解:结果页底部显示"已隐藏 N 道需要你没有的厨具的菜",可一键调整。

---

## ADR-010 · 热量数据标注为估算值

**日期:** 2026-08-21 · **状态:** 已确认

**选择:** 营养数据基于常用食材成分表按配方用量推算,产品内明确标注"估算值,误差约 ±15%"。

**理由:** 竞品(尤其 AI 生成类)把模型估的热量当精确数字展示,这是不诚实的。我们的数据是按配方成分推算的,方法可解释,但确实不是实验室测定 —— 同样一道红烧肉,肥瘦比例不同热量能差 30%。**标注不确定性比伪装精确更有用**:用户知道这是估算,就会把它当趋势参考而不是当计数依据,这才是正确的用法。

---

## ADR-011 · 不使用构建工具与外部依赖

**日期:** 2026-08-21 · **状态:** 已确认(ADR-001 的推论)

**选择:** 零 npm 依赖,零构建步骤,不用框架。原生 DOM 操作 + 模板字符串渲染。

**理由:** 是 ADR-001 的直接推论。同时这个数据量(153 道菜)和交互复杂度,原生实现完全可控 —— 引入框架的收益不足以抵消它对"双击即用"的破坏。

**代价:** 状态管理靠手写的 `S` + `save()` + `render()` 约定,需要纪律:**所有状态变更必须先改 `S`,再调 `save()` / `render()`,不允许直接改 DOM 后再补状态**。

为弥补"无构建期检查"这一代价(ADR-001),另外写了一个构建期校验脚本,强制检查:菜谱引用的食材与厨具 id 是否存在、菜谱 id 是否重复、用量与营养字段是否完整、标注热量与蛋白脂肪碳水推算热量的偏差、以及常用食材的主料覆盖度。这是零依赖方案下唯一的质量护栏,不能省。

---

## ADR-012 · 点击目标尺寸分三级,而非一律 44px

**日期:** 2026-08-22 · **状态:** 已确认

**背景:** PRD 原本写"可点区域 ≥ 44×44px"一刀切。实测发现分段控件和图标按钮只有 38px、行内小按钮 34px,不达标。

**选择:** 分三级 —— 主控件 44px / 行内次级动作 40px / 行内文字按钮 38px。已把分段控件(份数、营养目标、排序、辣度)和图标按钮从 38px 提到 **44px**,小按钮从 34px 提到 38px,库存和配餐行里的图标按钮单独收到 40px。

**理由:** 44px 来自 Apple HIG,针对的是**主要操作**。份数、营养目标、排序这些分段控件是高频点击项,必须给足 44px。而卡片行内的「看」「换」「✕」是低频次级动作,一律拉到 44px 会让列表行高膨胀 30%,一屏能看的菜从 10 道掉到 7 道 —— 为了一个很少误触的按钮牺牲核心信息密度是错的取舍。

**代价:** 严格意义上不完全符合 44px 通用建议。已在 PRD §9 明确写出分级标准和适用范围,不含糊过去。

---

## ADR-013 · 零输入时不做推荐,直接走引导

**日期:** 2026-08-22 · **状态:** 已确认(修复实测发现的缺陷)

**背景:** 实测发现:什么食材都不勾时,推荐结果里出现了 26 道「差一两样」。

**根因:** 所有「单主料 + 全基础包调味」的菜(如蒸南瓜、蛋炒饭),在零勾选时 `missMain=1`、`missSub=0`、`missSeason=0`,于是 `missTotal=1 ≤ 2` 且 `missMain=1 ≤ 1`,**恰好满足 Tier 1 的条件**。

**选择:** 在 `match()` 开头加零输入短路 —— `S` 为空时直接返回空列表。

**理由:** 用户什么都没选,系统说"差 1 样:南瓜"是**语义上的误导** —— 实际是什么都缺。分档的可信度是这个产品的核心资产(见 ADR-003),这种边界情况会直接损害它。零输入应该走引导页,并给出"只要一样就能做"的兜底建议,把用户推回到有意义的第一步。

**代价:** `match()` 多一个特殊分支。已在代码里写明为什么必须有它,避免以后有人当成冗余判断删掉。

---

## ADR-014 · 黑胡椒纳入调味基础包

**日期:** 2026-08-22 · **状态:** 已确认

**背景:** 基础包里有"胡椒粉"却没有"黑胡椒",两者被当成不同食材。结果是黑椒牛柳、烤鸡翅这类菜因为"缺黑胡椒"被压出 Tier 0。

**选择:** 把黑胡椒加进基础包,基础包从 20 样变 21 样。

**理由:** 内部不一致。家里有胡椒粉的人,几乎不会没有黑胡椒;把两者区别对待没有现实依据,只是制造噪音。同时保留 `SUBS` 里"黑胡椒 → 可用普通胡椒粉"的替代说明,关掉基础包时仍然有用。

---

## ADR-015 · 菜谱库从 120 道扩到 153 道

**日期:** 2026-08-22 · **状态:** 已确认

**背景:** 120 道做完后跑覆盖度审计,发现两个真问题:
1. **11 个食材完全没被任何菜用到** —— 娃娃菜、荷兰豆、红薯、秋葵、芦笋、培根、带鱼、鱿鱼、酸奶、平菇、黄豆。用户勾了等于白勾。
2. **13 个「常用」食材的主料出现次数不足 4 道**,违反 PRD §10 的覆盖度要求 —— 其中胡萝卜只在 1 道菜里当主料(却在 23 道菜里当配料),玉米、西兰花、生菜也只有 1 道。

**选择:** 补 29 道菜(总数 153),每道针对性地覆盖多个缺口,而不是随便凑数。

**备选:** 把 PRD 的覆盖度要求从"≥4 道主料"降到"≥3 道",让实现达标。

**理由:** 改指标去迁就实现是自欺欺人。这条要求的**真实目的**是"用户勾任何常见食材都能得到有意义的结果",降标准等于放弃这个保证。而且"勾了没菜"是最伤信任的失败模式 —— 用户不会认为是菜谱库小,只会认为这个应用不好用。

**代价:** 内容制作工作量增加约 25%。收益是明确的:32 个常用食材全部达到 ≥4 道主料,零"勾了没菜"的食材,且这两条现在由构建期校验脚本强制检查,以后加减菜谱不会悄悄退化。

---

## ADR-016 · 本地存储不可用时给出可见提示,而不是静默失败

**日期:** 2026-08-22 · **状态:** 已确认

**背景:** 某些环境会禁用 `localStorage`(Safari 无痕模式、`data:` 源、部分浏览器的 `file://` 策略)。原本 `save()` / `load()` 只是用 try/catch 兜住,应用照常能用,但刷新后数据全丢且用户毫不知情。

**选择:** 启动时探测存储可用性,不可用时在「选食材」和「我的」两页顶部显示一条明确提示,说明会丢数据以及怎么解决。

**理由:** 静默降级在这里是**有害的**。用户会花几分钟录入一屋子食材库存,刷新后全部消失且找不到原因 —— 这比一开始就告诉他"这个环境存不了"糟糕得多。降级可以静默,数据丢失不行。

---

## ADR-017 · 替代方案前缀用文字标签,不用箭头字形

**日期:** 2026-08-22 · **状态:** 已确认

**背景:** 缺料的替代方案原本用 `↳`(U+21B3)作前缀。在字体回退的环境下实测渲染成了近似 "l," 的乱码。

**选择:** 改成文字标签「替代 · 」。

**理由:** 单文件离线不能内嵌字体(见 ADR-006),就必须假设字形会落到系统字体上。冷僻的 Unicode 符号在这种前提下是不可靠的。文字标签在任何字体下都渲染正确,而且比箭头更明确地表达了"这是替代方案"。

---

## ADR-018 · 显隐用 opacity/transform 控制时,必须同时切 pointer-events

**日期:** 2026-08-22 · **状态:** 已确认(修复线上级 bug)

**背景:** 用户反馈"页面点击没反应"。整个应用的所有按钮全部失效。

**根因:** 抽屉的遮罩层 `#scrim` 是 `position:fixed; inset:0; z-index:60`,我只用 `opacity` 在 0 和 1 之间切换来控制显隐:

```css
/* 出问题的写法 */
.scrim{position:fixed;inset:0;z-index:60;opacity:0;transition:opacity .18s}
.scrim.on{opacity:1}
```

`opacity:0` 的元素**看不见,但依然参与命中测试**。于是一层不可见的全屏遮罩常驻在 z-index 60,把整页每一次点击都吃掉了。

同一个错误还有第二处:桌面断点(≥1024px)下 `.sheet` 是居中定位 + `opacity:0`,同样会挡住屏幕中央一大块。移动端因为它被 `translateY(101%)` 推到屏幕外,反而没暴露问题 —— 这也说明"看起来没事"是多么不可靠。

**修复:**

```css
.scrim{...;opacity:0;pointer-events:none;transition:opacity .18s}
.scrim.on{opacity:1;pointer-events:auto}
.sheet{...;pointer-events:none}
.sheet.on{transform:translateY(0);pointer-events:auto}
```

**为什么我的测试没抓到它 —— 这是更重要的教训**

之前的验收测试全部用 `element.click()` 程序化触发。**这种调用直接在元素上派发事件并向上冒泡,完全绕过浏览器的命中测试**。真实鼠标点击必须先命中最顶层元素,而那个位置上是遮罩。所以:

- 60 多项功能断言全部通过 —— 因为它们测的是"事件处理逻辑对不对"
- 而实际上没有一个按钮能被真人点到 —— 因为没有任何一项测的是"这个元素点得到吗"

`.click()` 验证的是**可达之后会发生什么**,验证不了**是否可达**。

**因此新增一条强制验收项:** 用 `document.elementFromPoint(控件中心)` 做真实命中测试 —— 检查该点上最顶层的元素是不是这个控件本身或其后代。同时做全屏覆盖物审计:扫出所有 `fixed`/`absolute`、覆盖视口 1/4 以上、`opacity < 0.05` 却仍然 `pointer-events: auto` 的元素。

修复前跑这套测试的结果是:**所有 48 个控件 100% 被 `DIV#scrim` 拦截**。这个信号极其明确,一开始就该跑。

**推论(以后写任何浮层都适用):** 用 `opacity` 或 `transform` 做显隐的元素,`pointer-events` 必须跟着一起切。只有 `display:none` 和 `visibility:hidden` 会自动退出命中测试 —— 做菜模式的 `.cook` 用的正是 `display:none`,所以它从一开始就没这个问题。

---

## ADR-019 · 只做手机端,不做桌面布局

**日期:** 2026-08-22 · **状态:** 已确认(方向收窄) · **推翻:** [ADR-006](DECISIONS.md) 中"桌面自适应"的部分

**背景:** v1.0 按"移动优先 + 桌面自适应"实现,维护三档布局:`<640px` 单列底部 Tab、`640–1024px` 两列顶部 Tab、`>1024px` 三列 + 详情居中弹层。用户明确要求:**只做手机端,不是 PC 端。**

**决定:** 只维护**一套**移动布局。桌面浏览器打开时,内容收进一条居中的手机宽度画框(`max-width: 480px`),画框外压暗底色。删除 `640px` 与 `1024px` 两个桌面断点。

**备选方案:**

| 方案 | 为什么不选 |
|---|---|
| 保留桌面自适应 | 用户明确不要。且每个新功能都要验两遍,而桌面那一遍没人用 |
| 桌面下直接显示"请用手机打开" | 太粗暴。开发和调试自己就在桌面上,而且用户偶尔会在电脑上翻菜谱 |
| 桌面下拉满宽度但仍用移动布局 | 单列内容拉到 1400px 宽会非常难看,行长远超可读上限 |

**理由:**

1. **真实使用场景在手机。** 站在冰箱前勾食材、在超市里看购物清单、在灶台边看步骤 —— 没有一个发生在电脑前。
2. **两套布局的成本不在 CSS 行数,在验证。** 桌面断点已经制造过一次真实事故:`≥1024px` 下 `.sheet` 居中 + `opacity:0` 挡住屏幕中央([ADR-018](DECISIONS.md)),而移动端因为抽屉被推到屏幕外反而没暴露。**这个 bug 完全是桌面布局带来的,而桌面布局没有用户。**
3. **画框方案让桌面仍然可用**,而且看起来是刻意设计,不是"没做适配"。开发调试也方便 —— 桌面上看到的就是手机上的样子。

**代价:**

- 桌面上一屏能看到的菜变少(从三列变一列)。接受 —— 桌面不是目标场景。
- 所有 `position:fixed; left:0; right:0` 的元素(`.tabbar` / `.stickycta` / `.sheet`)必须改成跟画框对齐,否则会横跨整个桌面视口。这是改造里最容易出错的一步,已列为 [ROADMAP](ROADMAP.md) P1 的验收项 #4。
- `.scrim` 遮罩**例外**,应该保持全屏 —— 它的作用是阻断画框外的交互。

**保留的媒体查询:** `max-width:400px` / `max-width:344px`(小屏字号收缩)和 `min-width:700px`(做菜模式大字号,服务大屏手机横屏)。这些都是移动端内部的适配,不构成第二套布局。

---

## ADR-020 · 搜索框必须留在静态区,输入时只重绘结果区

**日期:** 2026-08-22 · **状态:** 已确认(修复用户反馈的「搜不了中文」)

**背景:** 用户反馈食材搜索框打不出中文。

**根因:** 原实现在 `input` 事件里重绘整个 `#main`:

```js
S.q = ev.target.value;
$('#main').innerHTML = viewPick();      // ← 把 <input> 一起销毁重建了
$('#q').focus();
```

中文拼音输入法靠 **composition(组字)会话**工作:用户敲 `x-i-h-o-n-g`,这些字母先进入组字缓冲区,选定候选词后才产出「西红柿」。而每敲一个字母都触发一次 `input`,把 `<input>` 元素销毁重建 —— **组字会话随元素一起消失**,永远走不到选词那一步。

英文和拼音「看起来能搜」是因为它们不走组字,每个字符直接落进 value。所以这个 bug 只对中文可见。

**选择:** 把选食材页拆成静态区与动态区:

| 区域 | 内容 | 何时重绘 |
|---|---|---|
| 静态区 | 搜索框 | **永不**在输入时重绘 |
| `#chipWrap` | 已选食材 chip 条 | 勾选/取消时 |
| `#pickBody` | 分类导航 + 食材网格 | 输入、切分类时 |
| CTA 按钮 | 只改 `textContent` | 勾选/取消时 |

配套:
- `input` 事件里判断 `ev.isComposing`,组字中不过滤(否则会闪一屏「没找到」)
- 补 `compositionend` 监听,选词落定后再过滤一次
- 勾选食材、切分类、删 chip、清空 —— 全部改成局部刷新,**不再整页重绘**

**顺带解决的一个手机体验问题:** 原来搜「土豆」→ 点选 → 整页重绘 → 焦点丢失 → **手机键盘收起**,想搜下一样得重新点输入框。改成局部刷新后,可以连续「搜一样点一样」,键盘全程不收起。

**代价:** 渲染逻辑从「一把梭 `render()`」变成分区刷新,多了 `refreshPick` / `refreshChips` / `refreshCta` 三个函数,改动选食材页时要想清楚该刷哪一块。这是必要的复杂度 —— 输入框的生命周期不能由渲染逻辑随意决定。

**顺手加的能力:** 既然改了搜索,就让它同时支持**拼音**匹配 —— 食材的 `id` 本来就是无声调拼音(`tudou` / `xihongshi`),零成本。打分排序保证「中文名前缀 > 中文名包含 > 拼音前缀 > 别名 > 拼音中段」。

---

## ADR-021 · iOS / iPhone 适配的既定动作

**日期:** 2026-08-22 · **状态:** 已确认

**背景:** 用户在 iPhone 上使用,要求适配。iOS Safari 有一批固定的坑,与其每次现查,不如一次列清并固化为规范。

**逐条决定与理由:**

| 项 | 做法 | 为什么 |
|---|---|---|
| **输入框字号** | 一律 `16px` | iOS Safari 聚焦 `font-size < 16px` 的输入框会**强制放大整页**,且不会自动缩回。这是最常见也最难受的 iOS bug |
| **`100vh`** | 改 `100dvh`(保留 `100vh` 回落) | iOS 的 `100vh` 含地址栏高度,底部内容会被裁掉 |
| **安全区** | `viewport-fit=cover` + `env(safe-area-inset-*)` 用在顶栏、底栏、抽屉底、做菜模式、`main` 左右 | 刘海 / 灵动岛 / 底部横条;左右是为横屏 |
| **长按弹菜单** | 交互元素加 `-webkit-touch-callout:none` + `user-select:none` | 长按食材格会弹出「拷贝/查询」菜单和放大镜,很干扰。但**正文与步骤要保留可选中**,否则用户没法复制菜谱 |
| **点击延迟 / 双击缩放** | 交互元素加 `touch-action:manipulation` | 去掉 300ms 点击延迟 |
| **橡皮筋回弹** | `body` 加 `overscroll-behavior-y:none`;抽屉与做菜内容区加 `overscroll-behavior:contain` | 防止滚到底时把整页一起拖动 |
| **模态背景滚动** | 开抽屉/做菜模式时 `body{position:fixed}` + 记住偏移,关闭时还原 | iOS 上 `body{overflow:hidden}` **锁不住**滚动,只有 `position:fixed` 可靠 |
| **计时提示音** | 在「开始计时」的点击里 `ensureAudio()` 创建/恢复 `AudioContext` | iOS 要求 `AudioContext` 必须在**用户手势**中创建或 resume,否则计时结束时静默无声。原实现在计时结束那一刻才 `new AudioContext()`,在 iOS 上必然被拦 |
| **软键盘顶飞 fixed 元素** | 聚焦搜索框时给 `body` 加 `.kb`,隐藏吸底 CTA | iOS 键盘弹起时 `position:fixed` 元素会跟着乱跳 |
| **横屏字号自动放大** | `html{-webkit-text-size-adjust:100%}` | Safari 横屏会自作主张放大字号 |
| **搜索框原生样式** | `-webkit-appearance:none` + 干掉 `::-webkit-search-cancel-button` | iOS 会给 `type=search` 套一层原生圆角和清除按钮,和设计冲突 |
| **加到主屏** | `apple-mobile-web-app-capable` 等全套元信息 | 全屏运行、状态栏样式、主屏名称 |

**~~仍未解决的一项:~~** ~~`apple-touch-icon` 缺失,加到主屏会显示网页缩略图而不是图标。归入 P6。~~
✅ **已解决(会话 #7 / P8):** 生成了 180×180 的 `apple-touch-icon.png`(棕碗图标),`<head>` 里加了 `<link rel="apple-touch-icon">`。见 [ADR-022](#adr-022--部署到-gitee-pages方案-c-单分支腾讯云-cos-兜底)。注意 iOS **只认真实 PNG 文件、不认 `data:` URI**,所以图标必须与 `index.html` 同目录部署。

**Wake Lock:** iOS Safari 不支持 `navigator.wakeLock`,做菜模式屏幕仍会自动锁屏。代码已 try/catch 静默降级。这是平台限制,只能靠用户在系统里调长自动锁屏时间。

---

## ADR-022 · 部署到 Gitee Pages(方案 C·单分支),腾讯云 COS 兜底

**日期:** 2026-08-24 · 会话 #7 · 阶段 P8

**背景**
应用早就自足了(零外链、零 fetch、~250KB 单文件),但**分发是个死结**:微信不预览 `.html`,AirDrop 只限苹果之间,邮件/网盘都要"下载→手动挑浏览器打开"。普通人在 iPhone 上走不完。而且 `file://` 下 iOS Safari 很可能禁 localStorage、也没法加到主屏。托管成一个网址能**一次性**解掉分发 + 存储 + 主屏三件事。

**决策**

1. **仓库方案 C —— 同仓库单独部署分支 `gh-pages`。**
   `gh-pages` 是 orphan 分支,**只含 `index.html` + `apple-touch-icon.png`** 两个文件;`main` 保留全部开发文档。
   - 排除 A(全部公开):不想把踩坑记录一起公开(内容不敏感,但没必要)。
   - 排除 B(两个仓库):每次更新要同步两个仓库,比单分支更啰嗦。
   - C 的代价是分支管理稍复杂,已在 [DEPLOY.md](DEPLOY.md) 写死更新流程(`git checkout main -- index.html apple-touch-icon.png`)对冲。

2. **默认域名 `用户名.gitee.io/仓库名`。** 自定义域名要备案,先不折腾。

3. **主线 Gitee Pages,兜底腾讯云 COS 静态网站。**
   Gitee 国内访问好、免费,但**需实名**且**免费版每次更新要手动点「更新」**。万一实名卡住或政策再变,切 COS(国内最稳,一个 250KB 文件月费几分钱,且传了即最新、无手动更新步骤)。排除 Netlify/Vercel/GitHub Pages:国内访问不稳,朋友可能打不开。

4. **`apple-touch-icon` 用真实 PNG 文件,不用 `data:` URI。**
   iOS 历来**不认 `data:` URI 形式的 apple-touch-icon**,必须是可访问的真实文件。所以图标做成 `apple-touch-icon.png`(180×180)与 `index.html` 同目录部署,`gh-pages` 分支才要带上它。favicon 复用同一文件,不额外增文件。

**图标怎么来的(可复现)**
离线生成,不引任何依赖:手写 SVG(暖米色底 + 棕碗 + 热气,贴合品牌「无阴影 / 1px 实线」风格)→ headless Chrome 以 3× 渲染成 540×540 PNG → Pillow 用 Lanczos 缩到 180×180。命令记录见 [WORKLOG](WORKLOG.md) #016。

**影响**
- `index.html` `<head>` 增两行 `<link>`(apple-touch-icon + icon)。
- 新增 `apple-touch-icon.png`、`DEPLOY.md`。
- 新增本地 `gh-pages` 分支。
- 红线 1(零依赖、`file://` 下功能完整)**不破**:图标是纯静态文件,应用逻辑仍零网络请求;托管只是换了个 origin,`file://` 打开照样能用。

**遗留**
- 注册 / 实名 / 建仓库 / 开 Pages / 推送远端,必须用户自己做(要登录和身份验证)。验收 1–6 条待用户部署后在真机实测。
- 部署到 https 后,原「PWA 需 https 而我们是 file://」的两难消失,P6 可以真正谈 PWA 了。

### 修订 · 2026-08-24(同会话晚些):托管平台从 Gitee Pages 改为 EdgeOne Pages

**为什么改:** 代码推到 Gitee 后去开 Pages,发现仓库「服务」菜单里**根本没有 Gitee Pages**。查证:**Gitee 免费 Pages 已于 2024-05 整体下线,Pages Pro 也关闭了个人购买入口** —— 政策问题,对所有个人用户都没了,不是账号问题。正好命中本 ADR 原文里预留的"万一实名卡住或政策再变"那一手。

**新决策:**
- **主线换成腾讯云 EdgeOne Pages**(国内控制台 `edgeone.cloud.tencent.com`):完全免费、国内 CDN,且**支持直连我们已推的 Gitee 仓库**(`git push` 到 `gh-pages` 即自动重部署,没有 Gitee 那种"免费版每次手动点更新")。也支持直接拖文件上传。
- **只用 EdgeOne 分配的默认预览域名** —— 默认域名可直接访问、**不需要备案**;只有自定义域名在中国大陆加速区才要备案,不折腾。
- **COS 从"兜底"继续保留为兜底**(EdgeOne 也卡死、或默认域名在微信被拦时切它)。
- 仓库方案 C(`gh-pages` 只含两个部署文件)**不变**,反而更贴合 EdgeOne:生产分支填 `gh-pages`、输出目录 `./`,就只上线这两个文件,内部文档不带上去。

**未变的判断:** 选国内托管(而非 Vercel/Netlify/GitHub Pages)的理由不变 —— 朋友在国内、要在微信里打开,海外托管访问不稳。EdgeOne 与 COS 都是腾讯系、国内访问稳。

**新遗留:** 注册腾讯云+实名、EdgeOne 连仓库、选构建配置,都要用户在控制台自己点(我无法登录代操作)。部署方式与验收清单见 [DEPLOY.md](DEPLOY.md)。

### 再修订 · 2026-08-24(同会话):部署源从 Gitee 换成 GitHub

**为什么又改:** 在 EdgeOne Makers「导入 Git 仓库」里连 Gitee 后,**生产分支下拉框搜 `gh-pages` 显示「暂无数据」** —— EdgeOne 对 Gitee 的分支枚举有问题(GitHub 才是它一等公民)。

**决策:** 把同样的 `main` + `gh-pages` **加推一份到 GitHub `WangDuoYuOnebillion/EatWhat`**,EdgeOne 改**连 GitHub**;Gitee 仓库降为国内镜像/备份。本机 `gh` 未安装,GitHub 仓库由用户在网页建、我加 remote 后推送(凭据走本机 Git Credential Manager,我不经手密码)。

**影响:** 现在有两个远程 —— `github`(EdgeOne 部署源,推它触发重部署)+ `origin`(Gitee 镜像)。更新流程改为两个都推,见 [DEPLOY.md](DEPLOY.md)「以后怎么更新」。方案 C 与 `gh-pages` 分支结构不变。

### 终局修订 · 2026-08-24(同会话):正式托管定为 Cloudflare Pages ✅ 已上线

**为什么再改:** EdgeOne Makers 从 GitHub `gh-pages` 部署**构建成功了**,但成功弹窗写明:**默认域名链接仅提供 3 小时限时预览,且不支持去掉 `?eo_token=…` 直接访问,要长期用必须绑自定义域名。** 而自定义域名在中国大陆加速区**要 ICP 备案**(买域名 + 约 1–2 周)。也就是说 EdgeOne 的免费默认域名**根本不能拿来长期分享** —— 我此前"默认域名免备案能直接用"的判断,对 EdgeOne 不成立(那条只对已下线的 Gitee Pages 那种共享备案域名成立)。

**新决策(用户在选项里拍板):正式托管换成 Cloudflare Pages。**
- **免费、永久、无 token、无需备案**,从 GitHub `WangDuoYuOnebillion/EatWhat` 的 `gh-pages` 分支自动部署(`git push github gh-pages` 即重部署)。
- **已上线:** https://eatwhat-cui.pages.dev(我用 Claude for Chrome 在用户浏览器里建的项目;桌面已验 app 正常加载、无存储红条)。
- 构建配置:Framework preset = None,Build command 留空,Build output directory = `/`(根),生产分支 `gh-pages`。
- **EdgeOne 项目保留但降级**为"日后若买域名+备案,可换回国内 CDN"的选项;**COS 仍是另一条备选**。

**代价/风险(诚实记录):** Cloudflare 是**海外**服务器。选国内托管的初衷(微信里稳定打开)在这条路上**没有保证** —— 国内多数网络能打开、微信一般能进,但可能偶发慢/不稳。**是否可接受,取决于用户与家人的真机+微信实测结果**(P8 关闭条件)。若实测家人打不开,回退到"买域名+备案+EdgeOne"。

**这推翻了本 ADR 原文"排除 Vercel/Netlify 等海外托管"的判断** —— 当时的前提是"有国内免费且持久的选项"(Gitee Pages)。该前提已不存在:国内法规下,大陆服务器的公开持久网址必须备案,免费且免备案的持久托管只剩海外。所以在"零成本即时可用(海外)"与"要花钱等备案(国内)"之间,先取前者验证,不通再走后者。

**验收结果(用户真机实测,P8 收尾):**
- ✅ 手机浏览器 + 桌面都能正常打开 `eatwhat-cui.pages.dev` 并使用。
- ❌ **微信内点链接打不开** —— 海外未备案域名被微信拦截(典型行为)。过渡办法:微信里点右上角「···」→「在浏览器打开」通常可用。
- **用户决定接受现状,P8 完成(带此一条已知限制)。** 核心目标"有个能长期分享的免费网址"达成;"微信内一键直开"记为可选的后续任务 —— 唯一解是**备案的自定义域名**(买域名 + ICP 备案约 1–2 周 → 在已建好的 EdgeOne 项目上绑定)。这条不阻塞后续开发阶段。
- **教训延续:** 这也印证了本 ADR 反复踩的同一根钉子 —— "免费 + 国内直达"在当前政策下是不可能三角,要么花钱备案,要么接受海外线路的限制(此处即微信内打不开)。

---

## ADR-023 · 摆一摆的候选池:补到够,并把缺料写在卡上

**日期:** 2026-08-25 · 会话 #8 · 插在 P2 之前(ROADMAP「随时可做」· 修 bug 优先于阶段任务)

**背景**

用户报告:右上角 🎲「摆一摆」连点多次,只在**三道菜之间来回换**。

读码后定位到 `doRoll()` 的池子构造。它只取 tier 0(菜单页「现在就能做」那一档),
降级条件写的是 `if(!pool.length)` —— **上一档完全为空**才补下一档。

关键在于 tier 0 的门槛比看起来高得多:`missTotal === 0` 要求**主料 + 辅料 + 调味一样不缺**。
实测(默认偏好、调味基础包开):

| 勾选食材数 | tier0「现在就能做」 | tier1「差一两样」 |
|---:|---:|---:|
| 2 样 | **1** | 46 |
| 6 样 | **3** | 48 |
| 10 样 | **5** | 55 |
| 16 样 | **9** | 70 |

即使勾满 16 样,tier 0 也只有 9 道。"全空才补"意味着骰子永远在个位数的池子里打转 ——
用户点十次看到三道菜轮换,判断"坏了"是完全合理的。

**这是疏漏而不是设计取舍**,判据是同一份文件里三个同类功能的写法不一致:

| 功能 | 补候选的条件 |
|---|---|
| `openWeek()` 一周菜单 | `if(pool.length<6)` ← 不够就补 |
| `doDayPlan()` 一日配餐 | `if(pool.length<3)` ← 不够就补 |
| `doRoll()` 摆一摆 | `if(!pool.length)` ← **全空才补** |

同样的意图三种写法,骰子那种恰好是效果最差的一种。

**决策**

1. **池子 = 全部 tier 0 + 按分数补 tier 1(必要时 tier 2),补到至少 `ROLL_MIN = 12` 道。**
   tier 0 一道不漏;不够才补,且补的是分数最高(= 最接近能做)的几道。

2. **补的是「到够为止」,不是「整档吞下」。** 这一条是实测逼出来的:
   先写成"不够就把整档 tier 1 放进来",结果 6 样食材下池子 51 道,
   **40 次点击里只有 3 次是完全够料的** —— 骰子从"今天做什么"变成了"明天买什么"。
   改成补到 12 道后,同样条件下完全够料的比例回到 9/40,而勾满 16 样时是 **30/40**。
   也就是说:**勾得越多,骰子越倾向于直接就能做的菜**,这才是想要的退化曲线。

3. **摆出「差一两样」的菜时,卡片必须写出差什么。** 措辞与菜卡完全一致(`差 N 样:xxx、yyy`),
   警示色也复用。不加这一行的话,放宽池子等于悄悄递给用户一道做不了的菜 ——
   那比"只有三道菜"更糟。缺料信息从 `match()` 的返回里取(`missMain/missSub/missSeason`),
   `doRoll` 原来把它丢掉了,现在渲染时从 `mm.list` 里重新捞回来 ——
   顺带得到一个好处:事后补齐食材,这行提示会自己消失,不需要额外刷新。

**为什么没有"只补不缺主料的菜"这个中间选项**

开工前给用户的选项里有一条"只把 `missMain === 0` 的菜补进池子"(听起来更安全:缺的只是葱姜蒜这类)。
实测把它否掉了 —— 6 样食材下 tier 1 的 48 道里,**47 道都是缺 1 个主料,只有 1 道是纯缺辅料**:

```
缺主料1 缺辅料0 缺调味0 → 23 道
缺主料1 缺辅料1 缺调味0 → 18 道
缺主料1 缺辅料0 缺调味1 →  6 道
缺主料0 缺辅料1 缺调味0 →  1 道   ← 只有这一道
```

原因是**葱、姜、蒜都在调味基础包(21 样)里**,本来就不算缺。
所以"辅料齐了但主料缺"几乎是 tier 1 的全部形态,那个中间选项只会让池子从 3 道变成 4 道。
好消息是:缺的那"1 个主料"基本都是青椒 / 胡萝卜 / 菠菜 / 韭菜 / 豆角这类**极普通的菜**,
所以"差 1 样:青椒"是个有用的提示 —— 要么顺手买,要么本来就有只是忘了勾。

**影响**

- `index.html`:`ROLL_MIN` + `doRoll()` 池子构造;`viewDish()` 摆一摆卡片加缺料行;新增 `.roll .miss` 样式(原 `.miss` 只写在 `.dish` 下,够不到)
- `PRD.md`:F1 规格重写,验收表加 17b / 17c
- **摆一摆的承诺变了**:从"摆出来的一定完全够料"变成"摆出来的可能差一两样,但一定写清差什么"

**实测结果(headless 之外,真浏览器 375×812 驱动真实点击)**

| 项 | 修前 | 修后 |
|---|---|---|
| 40 次点击摆出多少不同的菜(6 样食材) | 3 | **12** |
| 其中完全够料 | 3/40 | 9/40 |
| 勾满 16 样时完全够料 | — | **30/40** |
| tier 0 的菜是否误显示缺料行 | — | 否 ✅ |
| 零勾选时点骰子 | 提示 + 跳回选食材 | 不变 ✅ |
| `#btnRoll` / `#rollAgain` / 底部 5 个 Tab 命中测试 | — | 全部可点 ✅(红线 4) |

---

## ADR-024 · 计时提示在 iOS 上的两条能力边界:静音不响(接受),后台冻结(要修)

**日期:** 2026-08-25 · 会话 #8 · 阶段 P2(iPhone 13 / iOS 18.7.8 / Safari / 线上 https)

**背景**

做菜模式的步骤计时器是唯一带"时间到了叫你一声"承诺的功能。P2 真机走查把它的
两条平台边界都测出来了 —— 结论相反,所以放在同一条 ADR 里对照记。

**边界一:静音键下不响 → 接受,不做兜底**

实测:手机拨到静音,计时到 0 **没有声音**。

原因是 iOS 把 Web Audio 归到"铃声"通道,物理静音拨钮会一起静掉 ——
这不是 `AudioContext` 没解锁的问题([ADR-021](#adr-021--ios--iphone-适配的既定动作) 那套"在用户手势里
创建/恢复 AudioContext"是有效的,正常音量下 C2 项实测会响)。**网页拿不到绕过静音键的能力**,
除非走原生 App 或推送通知,两条都超出本项目范围([ADR-001](#adr-001--单文件零依赖) 单文件红线)。

ROADMAP 原本写着"若不响,考虑加视觉提示兜底"。**用户决定不加,记为已知限制。**
理由(用户口径):做菜时人就在灶台边看着手机,静音本来就是自己选的状态。

⚠️ **给未来的自己:不要"顺手修"这一条。** 它不是漏做,是明确取舍。
真要动之前先回来推翻这条 ADR。尤其别去试那些绕静音的偏方(无声 `<video>` 保活、
`playsinline` + 空音轨之类)—— 它们要么在新版 iOS 失效,要么把电池吃掉,
而且会把"零依赖单文件"变成一堆平台 hack。

**边界二:切后台计时冻结 → 这是真 bug,要修(排到 P3)**

实测(用户口径):**2 分钟倒计时,点开始后切到别的 App,过了 60 秒再切回来,
计时器还显示 1 分 50 多秒** —— 60 秒真实时间里只走了约 10 秒。

原因是 `startTimer()` 用 `setInterval` **每秒把 `S.timerLeft` 减 1**,
计的是"tick 次数"而不是"时间"。iOS Safari 切后台后几乎立刻冻结 JS 定时器,
于是 tick 停了,倒计时也就停了。

**这一条和边界一的区别在于:它是我们自己的实现选择,不是平台限制。**
用挂钟时间(记录结束时刻 + `Date.now()` 反算)就没有这个问题 ——
所以它是 bug,不是"iOS 不支持"。

**决策:确认为 bug,但不在 P2 会话里改,排到 [P3](ROADMAP.md)。**
理由:P2 是走查阶段,职责是"给出明确结论"而不是"边测边改";
而 P3(手机原生手感)本来就要动做菜模式的交互,放一起改更省事。
已写进 ROADMAP P3 任务表的第 0 项(优先级最高),含具体修法,不会丢。

**影响面(修之前要知道的)**

按秒 tick 的实现同时影响两类场景,炖菜/煮面这种长时步骤最吃亏:
锁屏、切后台、甚至切到 Safari 的其他标签页都会让计时变慢。
**用户看到的表现是"计时器比灶上的实际时间慢",这比不响更危险** ——
不响只是没提醒,慢了会让人以为还没到时间。

**没有改的东西**

`flashTitle()`(计时结束闪浏览器标题)保留原样。它在手机上基本无效(看不到标题栏),
但成本为零,桌面浏览器下有用,而且不构成误导。修边界二时不用管它。

---

## 待定 / 未来需要决策的问题

- 菜谱库扩到 200+ 时,单文件是否还合适(当前 153 道 / 246KB,再翻一倍需要评估首屏解析耗时,可能要拆数据文件或改 IndexedDB)
- 是否需要 PWA 化以支持"添加到主屏幕" —— 归 [ROADMAP](ROADMAP.md) P6。**原"PWA 需 https 而我们是 file://"的两难已被 P8 消除**([ADR-022](#adr-022--部署到-gitee-pages方案-c-单分支腾讯云-cos-兜底)):托管到 https 后,加 service worker 只是成本问题,不再是路线冲突
- 一周菜单是否需要考虑营养均衡度的量化评分,而非只做荤素搭配
- 菜卡上是否要显示"这道菜能用掉你选的几样食材"的利用率数字(算法里已有该权重,但没暴露给用户)
- 步骤计时器目前只支持单个计时;炖菜类需要长时计时 + 后台提醒时,可能要引入 Notification API
