# STATE · 当前状态卡

> **这是跨会话交接的唯一权威。** 每次会话开头读它,结尾重写它。
> 如果它和别的文档冲突,以它为准,并顺手把别的文档订正掉。

**最后更新:** 2026-08-25 · 会话 #8 结束(P2 完成 + 修掉一个用户报的 bug)

---

## 一句话:现在在哪

应用是**「小付,今天吃什么?」v1.0**,纯手机端单文件 Web App,已上线 **https://eatwhat-cui.pages.dev**。
**✅ P2 手机真机走查完成** —— iPhone 13 / iOS 18.7.8 实测 10 项:8 过、1 已降级、1 已知限制、1 确认 bug(已排 P3)。
**「真机从未验过」这条长期已知限制,本次会话消掉了。**
**下一阶段:P6 加到主屏 / PWA —— 新开会话做。**

会话 #8 产出:
1. **修掉用户报的 bug:摆一摆只在三道菜之间打转**([ADR-023](DECISIONS.md) / WORKLOG #017)。已上线。
2. **P2 真机走查全部走完**([ADR-024](DECISIONS.md) / WORKLOG #018)。
3. 新发现一个真 bug:**计时器切后台会冻结**,用户决定排到 P3(不在本会话改)。

---

## 🟢 下个会话:开 P6 加到主屏 / PWA

按 [ROADMAP](ROADMAP.md) 执行顺序,P2 之后是 **P6**。

- P6 要做什么:`manifest.webmanifest`、全屏模式下的安全区实测、以及**开工时要问用户的一个决策:要不要 service worker**(见下面「待决问题 #2」)。
- `apple-touch-icon` **P8 已做完,跳过**。`<head>` 里 `theme-color` / `apple-mobile-web-app-*` / `format-detection` 也都已就位,**不要重复做**。
- 验收标准见 [ROADMAP.md](ROADMAP.md) P6 节:真机加到主屏后 —— 正确图标与名称、全屏无地址栏、顶部不被刘海压、底部不被小白条压、冷启动数据仍在。

**⚠️ 别顺手修计时器。** 它是 P3 的第 0 项,不是 P6 的活。一会话一阶段。

---

## ⚠️ 阶段编号 ≠ 执行顺序

阶段号只增不改。**"下一步做什么"看 [ROADMAP.md](ROADMAP.md) 的「执行顺序」表,不看编号大小。**

当前顺序:P0 ✅ → P5.1 ✅ → P1 ✅ → P4 ✅ → P8 ✅ → P2 ✅ → **P6 ← 下一个** → P3 → P5 → P9 → P7

---

## 阶段进度

```
P0   调研→构建→文档      ████████████ 100%  ✅ 会话 #1–#4
P5.1 改名+iOS+修搜索      ████████████ 100%  ✅ 会话 #6
P1   纯手机化改造         ████████████ 100%  ✅ 会话 #6
P4   校验脚本落盘         ████████████ 100%  ✅ 会话 #6
P8   部署到免费托管       ████████████ 100%  ✅ 会话 #7 上线 Cloudflare(微信内直开留待备案)
P2   手机真机走查         ████████████ 100%  ✅ 会话 #8 iPhone 13 / iOS 18.7.8

P6   加到主屏 / PWA       ░░░░░░░░░░░░   0%  ← 下一个会话
P3   手机原生手感         ░░░░░░░░░░░░   0%  ⚠️ 第 0 项是修计时器后台冻结(真 bug)
P5   菜谱库 153→200       ░░░░░░░░░░░░   0%
P9   微信小程序           ░░░░░░░░░░░░   0%  ⚠️ 大工程,且与红线 1 冲突
P7   v1.1 全量回归        ░░░░░░░░░░░░   0%
```

---

## 本次会话(#8)做了什么

会话开头用户报了个 bug,按 ROADMAP「随时可做 · 修 bug 优先于阶段任务」先修掉,再做 P2。

| # | 内容 | 记在哪 |
|---|---|---|
| ① | 修 bug:摆一摆候选池只有 3 道 → 改成补到够(`ROLL_MIN=12`)+ 卡片标缺料 | [ADR-023](DECISIONS.md) / WORKLOG #017 |
| ② | 推送上线:`main` `743f384` + `gh-pages` `e5e5d12`,线上验证 40 次点击摆出 12 道 | WORKLOG #017 |
| ③ | P2 真机走查 10 项逐条走完 | [ROADMAP](ROADMAP.md) P2 节 / WORKLOG #018 |
| ④ | 记下计时器两条边界:静音不响(接受)/ 后台冻结(要修,归 P3) | [ADR-024](DECISIONS.md) |

**修骰子时踩的坑(值得记住):** 第一版写成"不够就把整档 tier 1 吞下来",池子从 3 涨到 51,
**看着像修好了**,但 40 次点击只有 3 次是完全够料的 —— 骰子从"今天做什么"变成了"明天买什么"。
改成"按分数补到 12 道"才对。**池子大小不是目标,结果可用性才是。**

---

## 线上状态

| 项 | 值 |
|---|---|
| 线上地址 | **https://eatwhat-cui.pages.dev**(Cloudflare Pages,免费/永久/免备案) |
| 部署源 | GitHub `WangDuoYuOnebillion/EatWhat` 的 `gh-pages` 分支(remote 名 `github`) |
| 国内镜像 | Gitee `wangchen1995/eat-what`(remote 名 `origin`) |
| 当前线上版本 | `gh-pages` `e5e5d12`(含摆一摆修复),已验证与本地逐字节一致 |
| `main` | `743f384`,已推 github + origin |
| 更新方法 | 改完 `index.html` → 同步到 `gh-pages` → `git push github gh-pages`,Cloudflare 自动重部署。**完整流程见 [DEPLOY.md](DEPLOY.md)** |

---

## 待决问题

| # | 问题 | 卡住谁 | 我的倾向 |
|---|---|---|---|
| 2 | **要不要 service worker(离线缓存)** —— 应用零网络请求,离线价值不大 | **P6 ← 下个会话开工前要问用户** | 可做可不做,成本低。部署到 https 后原路线冲突已消 |
| 3 | **小程序走哪条路** —— 原生重写 vs `web-view` 套 H5 | P9 | 大概率原生:`web-view` 需已备案域名,个人主体走不通 |
| 4 | **小程序与单文件红线冲突** —— 需抽出共用数据+算法模块 | P9 | 开工前必须新增 ADR,不要绕过去 |
| 5 | `ROLL_MIN=12` 是拍的数 | — | 没做用户验证。日后觉得骰子重复就调大 |

---

## 已知限制(现状,不是待办)

- 🐛 **计时器切后台会冻结** —— 实测 2 分钟倒计时切出去 60 秒,回来只走了约 10 秒。
  `setInterval` 计的是 tick 次数不是挂钟时间,iOS 切后台冻结 JS。
  **这是唯一一条"确认是 bug 但还没修"的**,已排 [P3](ROADMAP.md) 第 0 项(含修法)。见 [ADR-024](DECISIONS.md)。
- **静音键下计时不响。** iOS 把 Web Audio 归铃声通道,网页拿不到绕过静音键的能力。
  **这是明确取舍不是待办** —— 用户决定不做视觉兜底。想动之前先推翻 [ADR-024](DECISIONS.md),
  **尤其别试绕静音的偏方**(无声 video 保活之类),会把单文件变成一堆平台 hack。
- **微信内打不开线上地址。** 海外未备案域名被微信拦。过渡:微信「···→在浏览器打开」。
  要一键直开只能上备案的自定义域名(可选任务,不阻塞任何阶段)。见 [DEPLOY.md](DEPLOY.md) 备选一。
- **Wake Lock 在 iOS Safari 不支持**,做菜模式仍会自动锁屏。已 try/catch 静默降级,平台限制。真机已验。
- **`file://` 下大概率存不住数据。** 页面顶部有红条兜底。**线上 https 无此问题**(真机已验)。
- **营养自洽只查"内部一致"**,查不出标称值整体偏高/偏低。3 条 ±18–19% 的 warning 属设计带内。
- 热量是估算值,误差约 ±15%。这是**设计上的诚实**,不是缺陷。
- ~~**真机从未验过。**~~ ✅ **会话 #8 消掉** —— iPhone 13 / iOS 18.7.8 走完 P2 全部 10 项。
- ~~**加到主屏图标真机没验。**~~ 图标本身已就位;**全屏模式的完整验收是 P6 的活**。

---

## 给未来的自己:四条最贵的教训

1. **`element.click()` 不能用来验证"点得到"。** 它绕过浏览器命中测试。验可点性只能用
   `document.elementFromPoint()`。详见 WORKLOG #010 / #016 / #017。

2. **先证伪测量工具,再信它的结论。** headless Chrome 视口下限钳在 500px、
   `--virtual-time-budget` 下 CSS 过渡不推进,都误导过判断。详见 WORKLOG #006。

3. **断言失败时,先怀疑断言本身,再怀疑代码。** 判据:**能说清失败的物理机制,就是真 bug;
   只能说"数字对不上",先查断言。** 会话 #8 用这条把"静音不响"(平台限制,接受)和
   "后台冻结"(实现选择,要修)分开了 —— 两者表现相似,处置相反。

4. **指标变好不等于问题解决。**(会话 #8 新增)修骰子时把池子从 3 道扩到 51 道,
   数字漂亮,但结果 90% 都要先买菜 —— 功能意义反而坏了。
   **改完要量"用户实际得到什么",不是只量"我改的那个数"。**
