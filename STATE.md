# STATE · 当前状态卡

> **这是跨会话交接的唯一权威。** 每次会话开头读它,结尾重写它。
> 如果它和别的文档冲突,以它为准,并顺手把别的文档订正掉。

**最后更新:** 2026-08-22 · 会话 #6

---

## 一句话:现在在哪

应用改名为 **「小付，今天吃什么？」**,已经是一个**纯手机端**应用:单一移动布局,桌面浏览器打开时收成居中 480px 手机画框,零桌面断点。

本次会话修掉一个真 bug(中文输入法搜不了字)、完成 iOS 适配 13 项、完成 **P1 纯手机化改造**、完成 **P4 校验脚本落盘**。

**下一个会话:做 P2「手机真机走查」—— 用户正在 iPhone 上用,这是唯一能验的时机,且必须用户配合。**

---

## 下个会话开工的第一件事

1. 读 [CLAUDE.md](CLAUDE.md)(红线)+ 本文件
2. 读 [ROADMAP.md](ROADMAP.md) 的 **P2 章节**
3. 向用户复述:「本次会话做 P2 手机真机走查,验收标准是 N 条,做完就停」
4. **P2 必须先问清楚这四件事**,它们决定后面怎么测:
   - 机型 + iOS 版本
   - 用 Safari 还是微信/其他内置浏览器
   - `index.html` 怎么传到手机的(AirDrop / 文件 App / 局域网 / 网盘)
   - 是直接打开文件,还是已经"加到主屏幕"
5. 然后让用户按清单实测并回报。**重点是这 7 项 headless 验不了的:**

   | # | 验什么 | 怎么看 |
   |---|---|---|
   | 1 | `file://` 下 localStorage 能否持久化 | 勾几样食材 → 杀掉浏览器重开 → 还在吗?不在的话页面顶部会有明确红条提示 |
   | 2 | 计时提示音 | 做菜模式点「开始计时」,等到 0,响不响 |
   | 3 | Wake Lock | 做菜模式停着不动,屏幕会不会自动锁(iOS 不支持,**预期会锁**,只要不报错就算过) |
   | 4 | 刘海 / 灵动岛 / 底部横条 | 顶栏文字有没有被刘海压住;底部 Tab 有没有被小白条盖住 |
   | 5 | 软键盘遮挡 | 点搜索框,键盘弹起后能不能看到搜索结果;吸底按钮应自动隐藏 |
   | 6 | 长按不弹菜单 | 长按食材格,不应弹出「拷贝/查询」和放大镜 |
   | 7 | 加到主屏 | 图标是什么样(**预期是网页缩略图,因为还缺 `apple-touch-icon`**,归 P6);打开后是否全屏无地址栏 |

---

## 阶段进度

```
P0 调研→构建→文档       ████████████ 100%  ✅ 会话 #1–#4
P1 纯手机化改造          ████████████ 100%  ✅ 会话 #6
P2 手机真机走查          ░░░░░░░░░░░░   0%  ← 下一个(必须用户配合)
P3 手机原生手感          ░░░░░░░░░░░░   0%
P4 校验脚本落盘          ████████████ 100%  ✅ 会话 #6
P5 菜谱库 153→200        ░░░░░░░░░░░░   0%
P6 添加到主屏/PWA        ░░░░░░░░░░░░   0%  ⚠️ 开工前需用户决策
P7 v1.1 全量回归         ░░░░░░░░░░░░   0%
```

> P4 本来排在 P2/P3 之后,提前做掉了 —— 它是 PRD §10 两条硬约束的唯一守卫,而当时脚本只存在于临时目录,多拖一个会话就多一分菜谱库悄悄退化的风险。

---

## 本次会话(#6)做了什么

**用户三条要求:** 改名、适配 iPhone 手机 UI、修「搜食材栏搜不了中文」。做完后补了 P1 和 P4。

| # | 内容 | 记在哪 |
|---|---|---|
| ① | **修中文搜索(真 bug)** —— 根因在渲染不在匹配 | [ADR-020](DECISIONS.md) · [WORKLOG #012](WORKLOG.md) |
| ② | **iOS 适配 13 项** | [ADR-021](DECISIONS.md) |
| ③ | **改名** —— 标题/页头/控制台/README + 两档小屏字号收缩 | WORKLOG #012 |
| ④ | **P1 纯手机化改造** —— 删三处桌面断点、480px 画框、fixed 元素收进画框 | WORKLOG #012 |
| ⑤ | **P4 校验脚本落盘** —— `check.js` + `--selftest` | WORKLOG #013 |
| ⑥ | **`git init`** —— main 分支、初始提交、pre-commit 钩子、LF 钉死 | WORKLOG #014 |

**两个真 bug 的根因,值得记住:**

- **中文搜不了**:`input` 事件里重绘整个 `#main`,把 `<input>` 销毁重建。中文输入法靠 composition 组字会话工作,元素一没,组字跟着没。英文/拼音"看着能用"是因为不走组字 —— 所以这个 bug **只对中文可见**。
- **最后一排食材格点不到**:吸底 CTA 是 `fixed` 不占文档流,`main` 底部留白 84px 但 Tab 栏 + CTA 遮住 120px,最后一排被永久盖住、滚也滚不出来。**截图上完全看不出来**(像是列表到底了),只有命中测试能抓到。

**验收实测**

- P1 六条标准全过:六个手机宽度 × 8 视图零横向溢出、命中率 100%、覆盖物零违规、桌面画框对齐 0.0px、零 `min-width` 媒体查询、37 项功能 + 22 项搜索断言零回归
- P4:`node check.js` 对当前数据零 error(3 条营养提醒都在 20% 阈值内);`--selftest` 注入 8 类错误 **9/9 通过**;另做端到端验证(真改坏一份文件副本 → 报 8 条 error、退出码 1)

---

## ⚠️ 一条流程债(下个会话请避免)

本次会话与 #5(规划会话)**并行发生**。我开工时 `CLAUDE.md` / `STATE.md` / `ROADMAP.md` 还不存在,所以**没有走「开头复述阶段并确认」的流程**,而是直接按用户三条要求动手,发现治理文档后才补做 P1、P4。

#5 的 STATE 里那条「index.html 被外部改动过,不是我改的」指的就是本会话。已核对:`index.html` 与分块构建产物**逐字节一致**,无第三方改动。

---

## 待决问题(需要用户回答)

| # | 问题 | 卡住谁 | 我的倾向 |
|---|---|---|---|
| 1 | **PWA 走哪条路?** A: 只做 iOS 加主屏 / B: 部署静态托管做完整 PWA / C: 保持本地,只补图标和全屏元信息 | P6 | **C** —— 应用零网络请求,service worker 的离线缓存是多余的;真正缺的只是 `apple-touch-icon` |
| 2 | **真机怎么配合?** 需要机型、iOS 版本、哪个浏览器、文件怎么传上去 | **P2(下一个会话,必须先答)** | 用户正在 iPhone 上用,直接问 |
| 3 | ~~`@media(min-width:700px)` 留还是删?~~ | ~~P1~~ | ✅ **已删**。加了 480px 画框后,视口 ≥700px 只意味着桌面,而做菜层被限成 480px 宽,31px 会撑爆 |
| 4 | ~~要不要 `git init`?~~ | ~~无~~ | ✅ **已做**(会话 #6)。`main` 分支,初始提交含全部 12 个文件;`check.js` 已挂 pre-commit,校验暂存区快照;`.gitattributes` 钉死 LF 行尾 |
| 5 | ~~`check.js` 要不要提前落盘?~~ | ~~P4~~ | ✅ **已做**(会话 #6) |

---

## 已知限制(不是待办,是现状)

- **真机从未验过。** 所有测试都在 headless Chrome 里跑(配 iframe 画框约束宽度)。P2 就是为了消掉这一条。
- **加到主屏没有图标。** `apple-mobile-web-app-*` 元信息已就位,但缺 `apple-touch-icon`,加到主屏会显示网页缩略图。归 P6。
- **Wake Lock 在 iOS Safari 不支持**,做菜模式屏幕仍会自动锁屏。代码已 try/catch 静默降级,这是平台限制,只能让用户在系统里调长自动锁屏。
- **`check.js` 只在本地提交时触发。** 已挂 pre-commit(需 `git config core.hooksPath .githooks` 启用),但没有 CI —— 仓库没有远端,也没有 CI 平台。真要 CI 得先决定推到哪。
- **营养自洽只能查"内部一致"**,查不出"标称值整体偏高或偏低"。真要校准得逐道对食材成分表核算,不在当前范围。
- 热量是估算值,误差约 ±15%,同一道菜肥瘦比不同能差三成。这是**设计上的诚实**,不是缺陷,不要试图"修"。

---

## 给未来的自己:三条最贵的教训

1. **`element.click()` 不能用来验证"点得到"。** 它直接在元素上派发事件,绕过浏览器命中测试。曾经 60 多项断言全绿,而实际上整个页面**一个按钮都点不到**(一层 `opacity:0` 但仍吃点击的全屏遮罩)。验可点性只能用 `document.elementFromPoint()`。详见 WORKLOG #010。

2. **先证伪测量工具,再信它的结论。** headless Chrome 视口下限钳在 500px → 误判"布局严重溢出";`--virtual-time-budget` 下 CSS 过渡不推进 → 误判"抽屉没滑上来"。两次都差点去修不存在的 bug。详见 WORKLOG #006。

3. **断言失败时,先怀疑断言本身,再怀疑代码。** 会话 #6 的 6 条 FAIL 里有 4 条是测试脚本写错(拿含滚动条的 `innerWidth` 比对 fixed 元素宽度、测 `display:none` 元素尺寸、记错配方用的是尖椒不是青椒、忘了桂皮香叶不在调味基础包)。

   但**不要滑向另一边** —— 另外 2 条是真 bug。判据是:**能说清失败的物理机制,就是真 bug;只能说"数字对不上",先查断言。**
