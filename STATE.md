# STATE · 当前状态卡

> **这是跨会话交接的唯一权威。** 每次会话开头读它,结尾重写它。
> 如果它和别的文档冲突,以它为准,并顺手把别的文档订正掉。

**最后更新:** 2026-08-22 · 会话 #6

---

## 一句话:现在在哪

应用是**「小付,今天吃什么?」v1.0**,纯手机端单文件 Web App,功能完整、验收全过、已进 git。

**下一个会话:做 P8「部署到免费托管(Gitee Pages)」。**

用户的真实需求是**把应用发给别人用**,而只发 `index.html` 在 iPhone 上基本走不通(微信不预览 `.html`)。托管成一个网址能一次性解决分发、localStorage 持久化、加到主屏三件事。

---

## ⚠️ 先看这个:阶段编号 ≠ 执行顺序

阶段号只增不改(否则 WORKLOG / DECISIONS 里的历史引用会集体失效)。
**"下一步做什么"看 [ROADMAP.md](ROADMAP.md) 的「执行顺序」表,不要看编号大小。**

当前顺序:P0 ✅ → P5.1 ✅ → P1 ✅ → P4 ✅ → **P8 ←下一个** → P2 → P6 → P3 → P5 → P9 → P7

---

## 下个会话开工的第一件事

1. 读 [CLAUDE.md](CLAUDE.md)(红线)+ 本文件
2. 读 [ROADMAP.md](ROADMAP.md) 的 **P8 章节**
3. 向用户复述:「本次会话做 P8 部署到 Gitee Pages,验收标准是 7 条,做完就停」
4. **先把这三个问题问掉**(P8 章节里有详细选项表,别边做边猜):

   | # | 问题 | 我的倾向 |
   |---|---|---|
   | 1 | 仓库要公开吗?Gitee Pages 免费版要求公开,内部文档会一起外露 | **C:同仓库开一个只含 `index.html` 的部署分支** |
   | 2 | 用 `用户名.gitee.io/仓库名` 这种默认地址行吗? | 行,自定义域名要备案,先不折腾 |
   | 3 | Gitee 万一不行(需实名/政策变动),备选走哪个? | 腾讯云 COS(国内最稳,月费几分钱) |

5. 然后:我做能做的(生成 `apple-touch-icon`、按选定方案整理仓库、写 `DEPLOY.md`),
   注册 / 实名 / 建仓库 / 开 Pages 这几步必须用户自己操作 —— 我给清单

---

## 阶段进度

```
P0   调研→构建→文档      ████████████ 100%  ✅ 会话 #1–#4
P5.1 改名+iOS+修搜索      ████████████ 100%  ✅ 会话 #6
P1   纯手机化改造         ████████████ 100%  ✅ 会话 #6
P4   校验脚本落盘         ████████████ 100%  ✅ 会话 #6
P8   部署到免费托管       ░░░░░░░░░░░░   0%  ← 下一个(需用户配合注册)
P2   手机真机走查         ░░░░░░░░░░░░   0%  (内容已因 P8 提前而重写)
P6   加到主屏 / PWA       ░░░░░░░░░░░░   0%
P3   手机原生手感         ░░░░░░░░░░░░   0%
P5   菜谱库 153→200       ░░░░░░░░░░░░   0%
P9   微信小程序           ░░░░░░░░░░░░   0%  ⚠️ 大工程,且与红线 1 冲突
P7   v1.1 全量回归        ░░░░░░░░░░░░   0%
```

---

## 本次会话(#6)做了什么

一次会话做了三个阶段 + 两件工程事,是流程债(见下),不是常态。

| # | 内容 | 记在哪 |
|---|---|---|
| ① | **修中文搜索(真 bug)** —— 根因在渲染不在匹配 | [ADR-020](DECISIONS.md) · WORKLOG #012 |
| ② | **iOS 适配 13 项** | [ADR-021](DECISIONS.md) |
| ③ | **改名**「小付,今天吃什么?」 | WORKLOG #012 |
| ④ | **P1 纯手机化改造** | WORKLOG #012 |
| ⑤ | **P4 校验脚本落盘** | WORKLOG #013 |
| ⑥ | **`git init`** + pre-commit 钩子 | WORKLOG #014 |
| ⑦ | **重构 ROADMAP** —— 加执行顺序表、插入 P8 部署、新增 P9 多端 | WORKLOG #015 |

**两个真 bug 的根因,值得记住:**

- **中文搜不了**:`input` 事件里重绘整个 `#main`,把 `<input>` 销毁重建。中文输入法靠 composition 组字会话工作,元素一没组字跟着没。英文/拼音"看着能用"是因为不走组字 —— 所以**只对中文可见**。
- **最后一排食材格点不到**:吸底 CTA 是 `fixed` 不占文档流,底部留白不够,最后一排被永久盖住、滚也滚不出来。**截图上看不出来**,只有命中测试能抓到。

---

## ⚠️ 一条流程债(下个会话请避免)

本次会话与 #5(规划会话)**并行发生**。我开工时 `CLAUDE.md` / `STATE.md` / `ROADMAP.md` 还不存在,所以没走「开头复述阶段并确认」的流程,直接按用户要求动手,发现治理文档后才补做 P1、P4。

已核对:`index.html` 与分块构建产物**逐字节一致**,无第三方改动。

**从 P8 起严格一会话一阶段。**

---

## 待决问题

| # | 问题 | 卡住谁 | 我的倾向 |
|---|---|---|---|
| 1 | **仓库公开性** —— Gitee Pages 免费版要求公开仓库,内部文档会外露 | **P8(下一个,必答)** | **C:同仓库开一个只含 `index.html` 的部署分支** |
| 2 | **托管平台兜底** —— Gitee 需实名且政策变动过 | **P8(必答)** | 备选腾讯云 COS,国内最稳、月费几分钱 |
| 3 | **要不要 service worker** —— 应用零网络请求,离线缓存价值不大 | P6 | 可做可不做,成本低。托管后再定 |
| 4 | **小程序走哪条路** —— 原生重写 vs `web-view` 套 H5 | P9 | 大概率只能走原生:`web-view` 需要已备案域名,个人主体走不通 |
| 5 | **小程序与单文件红线冲突** —— 需要抽出共用的数据+算法模块 | P9 | 开工前必须新增 ADR,不要绕过去 |
| ~~6~~ | ~~PWA 走哪条路~~ | ~~P6~~ | ✅ **已被 P8 吸收** —— 一旦托管到 https,原来那个"PWA 要 https 而我们是 file://"的两难就不存在了 |
| ~~7~~ | ~~要不要 git init~~ | — | ✅ **已做**(会话 #6) |
| ~~8~~ | ~~check.js 要不要落盘~~ | ~~P4~~ | ✅ **已做**(会话 #6) |

---

## 已知限制(不是待办,是现状)

- **真机从未验过。** 所有测试都在 headless Chrome 里跑(配 iframe 画框约束宽度)。设备是 **iPhone 13**(系统版本待确认)。P2 消掉这一条。
- **`file://` 下大概率存不住数据。** iOS Safari 对 `file://` 的存储策略很严。已有页面顶部红条兜底提示,不会静默丢数据。**P8 部署后此问题自动消失。**
- **加到主屏没有图标。** 元信息已就位,缺 `apple-touch-icon`。P8 或 P6 解决。
- **Wake Lock 在 iOS Safari 不支持**,做菜模式屏幕仍会自动锁屏。已 try/catch 静默降级,平台限制。
- **`check.js` 只在本地提交时触发。** 已挂 pre-commit(需 `git config core.hooksPath .githooks` 启用),无 CI —— 仓库没有远端。P8 推到 Gitee 后可考虑。
- **营养自洽只能查"内部一致"**,查不出标称值整体偏高/偏低。真要校准得逐道对成分表核算。
- 热量是估算值,误差约 ±15%。这是**设计上的诚实**,不是缺陷,不要试图"修"。

---

## 给未来的自己:三条最贵的教训

1. **`element.click()` 不能用来验证"点得到"。** 它绕过浏览器命中测试。曾经 60 多项断言全绿,而实际上整个页面**一个按钮都点不到**(一层 `opacity:0` 但仍吃点击的全屏遮罩)。验可点性只能用 `document.elementFromPoint()`。详见 WORKLOG #010。

2. **先证伪测量工具,再信它的结论。** headless Chrome 视口下限钳在 500px → 误判"布局严重溢出";`--virtual-time-budget` 下 CSS 过渡不推进 → 误判"抽屉没滑上来"。两次都差点去修不存在的 bug。详见 WORKLOG #006。

3. **断言失败时,先怀疑断言本身,再怀疑代码。** 会话 #6 的 6 条 FAIL 里有 4 条是测试脚本写错。

   但**不要滑向另一边** —— 另外 2 条是真 bug。判据是:**能说清失败的物理机制,就是真 bug;只能说"数字对不上",先查断言。**
