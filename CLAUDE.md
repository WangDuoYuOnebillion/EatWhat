# CLAUDE.md · 项目总纲

> **新会话必读。读完这一页,再去读 [STATE.md](STATE.md) 确认当前该干什么。**

## 这是什么

「今天吃什么」——**只解决一件事**:知道家里有什么食材,但不知道能做什么菜。

勾选现有食材 → 给出现在就能做的菜 → 附完整做法、每步耗时、热量营养、缺料提示。

**单文件离线 Web App**,只有一个 `index.html`,零依赖、零构建、无网络请求,数据存 localStorage。

## 目标平台:**只做手机端**

这是 2026-08-22 收窄的方向,**不是"移动优先"而是"只有移动"**。
桌面浏览器打开时按"手机画框"居中呈现,但**设计上只维护一套移动布局**。
详见 [ADR-019](DECISIONS.md)。

任何新增 UI,只需要在 320–500px 宽度下成立。不要再写桌面断点。

## 开发节奏:**一个阶段一个会话**

这是硬约定。不要在一个会话里推进多个阶段,不要"顺手把下一阶段也做了"。
阶段划分见 [ROADMAP.md](ROADMAP.md),当前进度见 [STATE.md](STATE.md)。

> ⚠️ **阶段编号 ≠ 执行顺序。** 编号只增不改(否则 WORKLOG / DECISIONS 里的历史引用会集体失效),
> 真正的先后看 [ROADMAP.md](ROADMAP.md) 顶部的「执行顺序」表。
> 比如 P8 排在 P2 前面 —— 因为部署会改变 P2 要测的内容。

## 文档分工

| 文档 | 职责 | 什么时候读 | 什么时候写 |
|---|---|---|---|
| **CLAUDE.md**(本文件) | 总纲、红线、会话流程 | 每次会话开头 | 红线或分工变化时 |
| [**STATE.md**](STATE.md) | **当前在哪、下一步做什么** | 每次会话开头(第二份) | **每次会话结尾必写** |
| [**ROADMAP.md**](ROADMAP.md) | 阶段划分与各阶段验收标准 | 开始一个新阶段时 | 阶段增删或范围调整时 |
| [**PRD.md**](PRD.md) | 要做什么 + 验收标准 + 算法/数据规格。**不含 UI 规范** | 实现功能前 | 需求或规格变化时**同步回填** |
| [**UI.md**](UI.md) | **界面设计规范的唯一权威** —— token、布局、断点、触控尺寸、视觉验收 | 动任何 UI 之前 | **改了 `:root{}`、断点、触控尺寸就必须同步** |
| [**DECISIONS.md**](DECISIONS.md) | 为什么这么选(ADR) | 想推翻某个既有做法之前 | 做出新的方向性选择时 |
| [**WORKLOG.md**](WORKLOG.md) | 做了什么、踩了什么坑 | 想知道某处历史时 | **每次实质变动后追加** |
| [**README.md**](README.md) | 给使用者看的说明 | — | 用户可见行为变化时 |
| **check.js** | 数据校验(7 类规则),已挂 pre-commit | 改完菜谱/食材 | 加新校验规则时 |

## 会话流程(每次都照做)

**开头**
1. 读 `CLAUDE.md` + `STATE.md`
2. 确认本次会话要做的阶段,以及该阶段的验收标准(在 `ROADMAP.md`)
3. 向用户复述一句"本次会话做 Pn:xxx,验收标准是 yyy",确认后再动手

**结尾(缺一不可)**
1. 跑该阶段的验收标准,把实测结果写下来
2. 追加 `WORKLOG.md` 一条(做了什么 / 为什么 / 影响文件 / 遗留)
3. **重写 `STATE.md`**:当前阶段、完成度、下一步第一件事、新增待决问题
4. 有规格变化 → 回填 `PRD.md`;有方向性选择 → 新增 ADR
5. 告诉用户"本阶段完成,下个会话从 Pn+1 开始"

## 红线(违反了就是 bug,不是风格问题)

1. **不引入任何外部依赖、构建工具、CDN、ES Module。** `file://` 下必须功能完整。
2. **不写桌面断点。** 只维护移动布局。
3. **显隐用 `opacity`/`transform` 控制时,必须同步切 `pointer-events`。** 这条曾导致整页点击失效,见 [ADR-018](DECISIONS.md) 和 WORKLOG #010。
4. **测可点性不能用 `element.click()`。** 它绕过命中测试。必须用 `document.elementFromPoint()` 验证元素真的点得到。
5. **热量必须标注为估算值。** 含烹调用油,误差 ±15%,不允许包装成精确数据。见 [ADR-010](DECISIONS.md)。
6. **首页只做「选食材 → 出菜」。** 不塞任何无关模块。这是竞品最大的教训。
7. **改了实现就回填文档。** 文档和实现对不上等于没有文档。
   **UI 规范只有 [UI.md](UI.md) 一处** —— 改了 `:root{}`/断点/触控尺寸,同步它,不要另写一份。见 [ADR-025](DECISIONS.md)。
8. **份数基准是 3 人份**(`q` 字段),`nu` 是每份营养不随份数变。加菜谱务必遵守。

## 关键实现坐标(`index.html`,约 4000 行)

| 找什么 | 搜什么 |
|---|---|
| 设计 token(规范见 [UI.md](UI.md) §2) | `:root{` |
| 食材库(122 项) | `var ING = [` |
| 菜谱库(153 道) | `var RECIPES = [` |
| 调味基础包 | `pk: 1` |
| 匹配算法 | `function match(` |
| 渲染入口 | `function render(` |
| 启动自检 | `console.warn` 附近的自检层 |
| 调试句柄 | `window.EW`(暴露 `S` / `RECIPES` / `ING` / `match()` / `render()`) |
